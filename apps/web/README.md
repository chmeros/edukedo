# @edukedo/web

React + TypeScript + Vite Frontend (siehe Architekturplanung Abschnitt 2, 3, 5).

## Stand

- **Minimales Grundgerüst:** Vite + React + TypeScript, tRPC-Client (`@trpc/react-query` + TanStack Query v4) typsicher gegen `@edukedo/api` verdrahtet (`src/trpc.ts`).
- **PWA-Grundgerüst (F-40/F-41):** `vite-plugin-pwa` (`vite.config.ts`) — Web-App-Manifest + App-Shell-Precaching, installierbar. Platzhalter-Icons unter `public/icons/`. Volle Offline-Synchronisierung (F-42) ist bewusst nicht Teil davon, siehe Architekturplanung Abschnitt 13.
- **Offline-Modus (F-42), vollständig umgesetzt (alle sechs Bausteine):** `src/offlineDb.ts` legt das lokale IndexedDB-Schema über Dexie.js an (`content`: offline verfügbare Karteikarten/Quiz-Items inkl. Lösung; `queue`: noch nicht synchronisierte Lern-Ereignisse mit Zeitstempel + Client-UUID). Der FSRS-Scheduler und die Quiz-Prüflogik liegen dafür jetzt in `@edukedo/shared` (`fsrs/scheduler.ts`, `quiz-logic.ts`), damit Client und Server denselben Code nutzen. `src/OfflineDownload.tsx` (im "Fortschritt"-Tab) lädt per Klick über `offline.downloadKurs` den vollständigen Karteikarten-/Quiz-Bestand des aktiven Kurses in `offlineDb.content` — ein erneuter Download aktualisiert bestehende Einträge statt sie zu duplizieren. `src/useOnlineStatus.ts` erkennt einen Verbindungswechsel; `Flashcards.tsx`/`Quiz.tsx` lesen dann aus `offlineDb.content` statt vom Server und werten Antworten lokal aus (`src/offlineFlashcards.ts`/`src/offlineQuiz.ts`), inkl. Eintrag in die Sync-Warteschlange. `src/offlineSync.ts` überträgt diese Warteschlange über `offline.syncQueue`, sobald `src/useOfflineSync.ts` einen Wechsel zu `online` erkennt (automatisch, ohne eigene Nutzer-Interaktion) — nur die vom Server bestätigten Einträge werden danach lokal gelöscht, alles andere bleibt für einen späteren Versuch stehen. `src/OfflineStatus.tsx` (im Header, siehe `App.tsx`) macht das sichtbar: "Offline"-Hinweis inkl. Live-Zähler ausstehender Ereignisse (`src/useOfflineQueueCount.ts`, reaktiv über Dexie-Table-Hooks statt Polling), "Wird synchronisiert…", eine kurz sichtbare "✓ Synchronisiert"-Bestätigung sowie ein bleibender Fehlerhinweis bei nicht übernommenen Ereignissen. Siehe Architekturplanung Abschnitt 13 für den vollständigen Architekturansatz.
- **Auth-Seite:** Register-/Login-Formular gegen die bestehenden `auth.register`/`auth.login`/`auth.logout`/`auth.me`-Endpunkte (`src/App.tsx`).
- **Design-System (`src/styles.css`):** Design-Tokens (Farben inkl. Dark-Mode-Varianten, Schriften Fredoka/Manrope/JetBrains Mono) aus dem Entwurf `design/01-landing-und-app-vorschau.html` übernommen (siehe `design/README.md` und Architekturplanung Abschnitt 13) — kein Tailwind/shadcn, reines CSS mit Custom Properties.
- **Öffentliche Landing Page:** `src/LandingPage.tsx` — Marketing-Startseite (Hero, Mission, Kursvorstellung, Methode) für nicht eingeloggte Besucher:innen, als lokaler Anzeigezustand in `App.tsx` verdrahtet (`showAuth`), nicht als eigene Route. CTAs wechseln auf das bestehende Login-/Registrierungsformular. Siehe Architekturplanung Abschnitt 13.
- **Globaler Header (Layout-Vereinheitlichung, siehe Architekturplanung Abschnitt 13):** `src/Header.tsx` — dieselbe Kopfzeile (Logo + rechte Aktionen) auf wirklich allen Seiten, nicht nur der Landing Page: eingeloggte App, Login/Registrierung, Eltern-Dashboard, Vorschau, Rechtsseiten. `src/GuestHeaderActions.tsx` liefert die rechten Aktionen (Anmelden/Kostenlos starten) für alle nicht eingeloggten Zustände. Löst den zuvor nur teilweise behobenen Bruch zwischen der offenen Landing-Page-Kopfzeile und der in eine einzelne `.shell > .card` gepressten restlichen App ab.
- **Flip-Card (`src/FlipCard.tsx`):** Wiederverwendbare 3D-Flip-Interaktion (Frage-/Antwortseite), genutzt von `Flashcards.tsx` (ersetzt den früheren "Antwort zeigen"-Button) und von der Landing Page (Beispiel-Karteikarte im Hero). Design-Vorschlag, siehe Architekturplanung Abschnitt 13: optionaler `stacked`-Prop zeigt zwei angedeutete, rotierte Karten dahinter (Karteikasten-Optik, `--sprout-tint`/`--sun-tint`) — nur von `Flashcards.tsx` gesetzt, wenn mehr als eine Karte fällig ist; die Landing-Page-Demo bleibt unverändert.
- **Konto-Selbstlöschung (F-06):** `src/DeleteAccount.tsx` — Bestätigungsdialog mit Passworteingabe, ruft `auth.deleteAccount`.
- **Mehrfach-Kursbelegung aktiv genutzt (F-09):** `src/CourseSwitcher.tsx` — jetzt ein Dropdown im globalen Header (`.header-menu`, siehe Layout-Vereinheitlichung) statt großer Kacheln im Hauptbereich; eingeschriebene Kurse zur Auswahl, weitere veröffentlichte Kurse zum Beitreten. Der aktive Kurs (`App.tsx`, `selectedKursId`, fällt automatisch auf den ersten eingeschriebenen Kurs zurück) wird als `kursId`-Prop an Theorie/Karteikarten/Quiz/Fortschritt durchgereicht — alle vier zeigen jetzt nur noch Inhalte/Fortschritt des ausgewählten Kurses statt über alle eingeschriebenen Kurse hinweg zu aggregieren. Siehe Architekturplanung Abschnitt 13. Redesign-Audit-Nachtrag: beide Listen (belegte/verfügbare Kurse) nutzen jetzt `.list-row` statt der früheren `.course-tile`/`.join-row`-Boxen; ein belegter Kurs ist als ganze `.list-row`-Zeile klickbar, der aktive Kurs wird über `.list-row.is-active` (Textfarbe) plus Häkchen markiert.
- **Nutzer-Menü im Header:** `src/UserMenu.tsx` — bündelt Rolle/Logout/Konto-löschen (löst die bisherige, mitten im Lernbereich stehende "Eingeloggt als ..."-Zeile ab) sowie, nur für `role === "admin"`, den Umschalter in den separaten Admin-Bereich. `src/useClickOutside.ts` schließt Header-Dropdowns (Kurs-/Nutzer-Menü) bei Klick außerhalb.
- **Karteikarten-Modus (F-20):** `src/Flashcards.tsx` — fällige Karten anzeigen, per Flip-Card (`src/FlipCard.tsx`) umdrehen, Selbsteinschätzung (nicht gewusst/unsicher/gewusst) an `progress.submitReview` melden.
- **Theorie-Ansicht:** `src/Theorie.tsx`, neuer Tab — Themen-Auswahl (TOC) plus Fließtext-Anzeige (`content.theorySections`); einfacher, selbst geschriebener Markdown-zu-JSX-Renderer für `###`-Zwischenüberschriften und `**fett**` statt einer zusätzlichen Bibliothek, da der Theorie-Content nur diese zwei Konstrukte nutzt.
- **Quiz-Modus (F-21, alle vier Formate):** `src/Quiz.tsx` — Multiple Choice (Option wählen), Zuordnung (Paare per Klick bilden), Lückentext (Inline-Eingabefelder), Kurzantwort (freies Textfeld); Server prüft und liefert Feedback, Abschluss-Score über alle Formate hinweg. Die vier Schritt-Komponenten liegen in `src/QuizSteps.tsx` und werden auch von `Vorschau.tsx` wiederverwendet (siehe unten).
- **Fortschrittsanzeige (F-30), inkl. Quiz-Ergebnissen (F-26):** `src/Progress.tsx` — Fortschrittsbalken je Fachgebiet und (eingerückt) je Thema, mit Prozent "beherrscht" und Bruchzahl; zählt jetzt Karteikarten und alle vier Quiz-Formate zusammen. `src/Quiz.tsx` invalidiert `progress.overview` nach jeder Antwort, damit der Fortschritt-Tab nicht auf einem veralteten Stand hängen bleibt.
- **Restzeit-/Lernpensum-Anzeige (F-35):** `src/Zielplanung.tsx`, oben im "Fortschritt"-Tab — zeigt je nach Kurs-Zielmodus (`kurs.targetMode`) entweder einen Zieltermin-Countdown ("einzeltermin": verbleibende Themen ÷ verbleibende Wochen, Rückstands-Warnung, Formular für Zieltermin/Plan-Start) oder ein wiederkehrendes Wochenziel ("wochenziel": Formular für ein Lerneinheiten-Pensum pro Woche, Fortschrittsbalken gegen die letzten rollierenden 7 Tage). Beide Modi laufen über `progress.pacing`, das Setzen der Zielwerte über `courses.setTarget`. Siehe Architekturplanung Abschnitt 13.
- **Barrierefreiheit (F-44), punktueller Durchgang über wiederverwendete Bausteine:** Sprungmarke zum Hauptinhalt (`.skip-link` in `Header.tsx`, zielt auf `id="main-content"` auf jedem `<main>`). Die drei Tab-/Segmented-Leisten (Lernmodus in `App.tsx`, Login/Registrieren ebenda, Prüfungsvorbereitung in `Pruefungsvorbereitung.tsx`) nutzen jetzt das volle ARIA-Tablist-Muster mit roving Tabindex und Pfeiltasten-Navigation (`src/tabListKeyboardNav.ts`, gemeinsam genutzt). Header-Dropdowns (Kurs-/Nutzer-Menü) schließen jetzt auch per Escape, mit Fokus-Rückgabe an den Auslöser (`src/useDismissableMenu.ts`, vormals `useClickOutside.ts`). `FlipCard.tsx` blendet die jeweils unsichtbare Kartenseite per `aria-hidden` auch für Screenreader aus (vorher lagen Frage und Antwort unabhängig vom Umdreh-Zustand gleichzeitig im Accessibility-Tree) und trägt `aria-pressed`. Elf verstreute Fehlertexte zu `src/ErrorMessage.tsx` (`role="alert"`) zusammengeführt. Neue, dediziert für weißen Text auf Flächen kalibrierte Farbtokens (`--coral-fill`/`--sprout-fill`/`--danger-fill`) lösen einen WCAG-AA-Kontrastverstoß bei Haupt-CTA-Button/aktiven Tabs/Karteikarten-Rückseite, ohne die vielfach wiederverwendeten Basisfarben selbst zu ändern. Siehe Architekturplanung Abschnitt 13.
- **Eltern-Consent-Flow (F-08, Kernmechanismus):** Registrierungsformular fragt bei unter 16-Jährigen zusätzlich die E-Mail eines Elternteils ab (`src/App.tsx`, `needsParentEmail`) und zeigt nach dem Absenden einen Sperrhinweis inkl. Dev-Bestätigungslink (`devConfirmUrl`, solange kein echter E-Mail-Versand angebunden ist). `src/ConsentConfirm.tsx` ist die öffentliche, loginlose Zielseite `/consent/confirm?token=...` des Bestätigungslinks (bestätigt, loggt den Elternteil automatisch ein und verlinkt weiter zum Eltern-Dashboard), direkt per `window.location.pathname`-Weiche in `src/main.tsx` gerendert (kein eigener Router im Projekt).
- **Eltern-Dashboard-Grundgerüst (F-90):** `src/ParentDashboard.tsx`, Route `/parent` — Login-Formular (falls noch keine Parent-Session), einmaliges Setzen eines eigenen Passworts (falls `passwordSet === false`), danach Liste verknüpfter Kinder mit Einwilligungsstatus und Widerruf-Button (mit Bestätigungsschritt, analog zu `DeleteAccount.tsx`).
- **Kindgerechte Datenschutz-Kurzfassung (F-53):** `src/DatenschutzKinder.tsx`, statische Seite unter `/datenschutz-kinder`, verlinkt aus Registrierungsformular, Sperrhinweis und Eltern-Dashboard. Bewusst als ungeprüfter Entwurf gekennzeichnet, da die vollständige juristische Fassung (F-51) im Projekt noch nicht existiert, siehe Architekturplanung Abschnitt 13.
- **Kontoloser Vorschau-Modus (F-08):** `src/Vorschau.tsx`, Route `/vorschau` (kein Login) — 5 zufällige Demo-Fragen aus allen veröffentlichten Kursen, verlinkt vom Sperrhinweis nach Registrierung einer/eines Minderjährigen. Nutzt dieselben Schritt-Komponenten wie `Quiz.tsx` (`src/QuizSteps.tsx`), nur gegen die öffentlichen `preview.*`-Endpunkte statt `quiz.*`.
- **Admin-/Redaktionsbereich, erste Version (F-11):** `src/AdminPanel.tsx` — Kursliste mit Veröffentlichen/Zurückziehen-Button je Kurs, plus ein "Content neu importieren"-Button (F-17, ruft `admin.triggerImport`), nur sichtbar für `role === "admin"` (`App.tsx`, kein eigener Account-Typ, keine eigene Route). Löst den bisherigen Weg ab, `kurs.is_published` und den Content-Import ausschließlich per SQL/CLI zu setzen bzw. auszulösen. Seit der Layout-Vereinheitlichung (siehe Architekturplanung Abschnitt 13) eine eigene Ansicht (`App.tsx`-`view`-Zustand, umgeschaltet über `UserMenu`) statt eines Inline-Anhängsels über der Kursliste.
- **Dev-Proxy:** `vite.config.ts` leitet `/api` an `http://localhost:3001` (apps/api) weiter, damit Session-Cookies im Dev-Modus ohne CORS-Klimmzüge funktionieren.
- **Business-Lizenzen, Auth-Grundgerüst (F-91, Baustein 1 von 6, Iteration 6):** `src/CompanyDashboard.tsx`, Route `/company` (analog zu `ParentDashboard.tsx`) — Login-Formular (falls noch keine Company-Session), einmaliges Setzen eines eigenen Passworts (falls `passwordSet === false`), danach Kontodaten (Name, Sitzplatz-Kontingent, Abrechnungsstatus). `src/CompanySetup.tsx`, Route `/company/setup?token=...` (analog zu `ConsentConfirm.tsx`) ist die öffentliche, loginlose Zielseite des vom Admin ausgelösten Setup-Links. `AdminPanel.tsx` bekam einen neuen Bereich zum Anlegen von Unternehmens-Konten (inkl. Anzeige des Setup-Links, solange kein echter E-Mail-Versand angebunden ist). Branding (F-92) und aggregierte Statistik (F-93) sind eigene, spätere Bausteine.
- **Business-Lizenzen, Einladungscodes + Mitgliedschaft (F-91, Baustein 2 von 6):** `CompanyDashboard.tsx` bekam zwei neue Bereiche — Einladungscodes erstellen/widerrufen und Teilnehmende-Liste (E-Mail/Beitrittsdatum) mit Lizenzentzug, dazu ein "X von Y belegt"-Sitzplatz-Zähler in `company.me`. Neue `src/RedeemCompanyCode.tsx`, im "Fortschritt"-Tab (`Progress.tsx`) neben Zielplanung/Offline-Download platziert, da es sich um eine kontobezogene, nicht lernmodus-spezifische Einstellung handelt — löst `company.redeemInviteCode` aus.
- **Business-Lizenzen, Branding (F-91/F-92, Baustein 3 von 6):** `CompanyDashboard.tsx` bekam eine neue `BrandingSection` (Logo-URL/Farbe/Begrüßungstext, mit Live-Vorschau) zum Pflegen von `company.updateBranding`. Neue `src/CompanyBranding.tsx` zeigt das Branding des zugeordneten Unternehmens (`company.myBranding`) als Banner oberhalb der Lernmodus-Tabs in `App.tsx` — bewusst ohne Interaktivität/Tracking, reiner Reputationswert. `RedeemCompanyCode.tsx` invalidiert `company.myBranding` nach erfolgreichem Einlösen, damit das Banner sofort erscheint, ohne dass die Seite neu geladen werden muss.
- **Business-Lizenzen, aggregierte Statistik (F-91/F-93, Baustein 4 von 6):** `CompanyDashboard.tsx` bekam eine neue `StatsSection` (Anteil aktiver Lizenzen, Ø Trefferquote, Ø Fortschritt aus `company.stats`) im selben `stat-section`/`stat-tile`-Vokabular wie "Deine Lernstatistik" in `Progress.tsx`. Unterhalb der serverseitigen Mindestgröße zeigt sie einen erklärenden Hinweistext statt Kennzahlen.
- **Business-Lizenzen, Sponsoring (F-91/F-94, Baustein 5 von 6):** `AdminPanel.tsx` bekam eine neue Sektion "Admin: Sponsoring (F-94)" (Anlage-Formular inkl. Kurs-Auswahl "Plattformweit"/einzelner Kurs, Aktivieren/Deaktivieren analog zum Kurs-Veröffentlichen-Muster). Neue `src/SponsorBanner.tsx` zeigt alle zutreffenden Sponsorings (`sponsor.list`) oberhalb der Lernmodus-Tabs in `App.tsx` — bewusst ohne jede Interaktivität/Personalisierung (N-01/N-13), sichtbar für ALLE Lernenden, nicht nur Unternehmens-Mitglieder.
- **Business-Lizenzen, Admin-Freischalt-Werkzeug für die Abrechnung (F-91, Baustein 6 von 6):** `AdminPanel.tsx` bekam eine neue `CompanyBillingForm` je Zeile der Unternehmens-Konten-Liste (Dropdown Ausstehend/Aktiv/Abgelaufen + Sitzplatz-Kontingent-Eingabe + Speichern-Button, ruft `admin.updateCompanyBilling` auf).
- **Einladungs-/Freundschaftssystem, Grundgerüst (F-63, Iteration 6, Social-Cluster):** Neue `src/FriendCircle.tsx`, im "Fortschritt"-Tab (`Progress.tsx`) neben Zielplanung/Offline-Download/Unternehmenscode-Einlösung platziert — je Kurs getrennt (`kursId`-Prop). Einladungscodes erstellen/auflisten/widerrufen (`friend.createInviteCode`/`inviteCodes`/`revokeInviteCode`), ein Code einlösen (`friend.redeemInviteCode`, Erfolgsmeldung mit E-Mail der/des neuen Freundes) und eine Freundesliste (`friend.friends`) für den jeweiligen Kurs. Highscore (F-60), Duelle (F-61), Lernpartner-Vermittlung (F-62) — die eigentliche Nutzung des hier aufgebauten Freundeskreises — sind eigene, spätere Bausteine.
- **Melde-/Blockier-UI, zunächst innerhalb des Freundeskreises (F-68/F-63):** `FriendCircle.tsx` bekam je Freund "Melden" (blendet ein Begründungsfeld ein) und "Blockieren" (sofort per Klick, analog zu "Widerrufen"/"Lizenz entziehen"), plus eine neue "Meine Blockierungen"-Sektion mit Entblocken-Button. `AdminPanel.tsx` bekam eine neue Sektion "Admin: Meldungen (F-68)" (offene Meldungen mit Reporter-/Reported-E-Mail, Begründung, Kurs; Schließen-Button).
- **Highscore-/Punkteliste (F-60):** Neue `src/Highscore.tsx`, im "Fortschritt"-Tab (`Progress.tsx`) nach `FriendCircle` platziert — Opt-in-Kontrollkästchen (`highscore.setOptIn`/`myOptIn`) und Bestenliste (`highscore.leaderboard`, beschränkt auf den eigenen Freundeskreis). Zeigt für minderjährige Nutzer:innen einen Hinweistext statt des Kontrollkästchens (F-66, `isMinor`-Prop aus `App.tsx`/`Progress.tsx` durchgereicht).
- **Lernpartner-Vermittlung (F-62):** Neue `src/Lernpartner.tsx`, im "Fortschritt"-Tab nach `Highscore` platziert — Auswahlfeld für den bevorzugten Handlungsbereich (`lernpartner.setFachgebiet`, nutzt die bereits über `progress.overview` geladene Fachgebiets-Liste) und eine Übereinstimmungsliste (`lernpartner.matches`) mit Badges für "Ähnlicher Zieltermin"/"Gleicher Handlungsbereich". Rein anzeigend, kein Chat.
- **Nicht-soziale Gamification (F-67):** Neue `src/Achievements.tsx`, im "Fortschritt"-Tab nach `Lernpartner` platziert — bewusst OHNE `kursId`-Prop (kontoweit statt kursskopiert). Ruft `gamification.checkAndAward` beim Mounten auf und zeigt bei neu erreichten Achievements eine Erfolgsmeldung; darunter vier Bestwert-Kacheln (`gamification.myPersonalBests`) und die vollständige Achievement-Liste (`gamification.myAchievements`, erreicht/nicht erreicht).
- **Redesign-Audit: vertikale Zentrierung bei Karteikarten/Quiz/Prüfung-Setup auf breiten Bildschirmen, siehe Architekturplanung Abschnitt 13:** `.content-narrow` wird ab 860px zu einem Flex-Container mit `justify-content: center`/`min-height: 60vh` — eine einzelne Karteikarte/Quizfrage blieb vorher oben in der breiten `.shell` "liegen", mit viel ungenutzter Fläche darunter. Degradiert automatisch zu normalem Top-Alignment bei längerem Inhalt (z. B. Präsentationstrainer). Mobile unverändert Top-aligned.
- **Redesign Phase 2: Admin-Panel, Eltern-Dashboard, Unternehmens-Dashboard, siehe Architekturplanung Abschnitt 13:** `AdminPanel.tsx` und `CompanyDashboard.tsx` (eingeloggte Ansicht) auf volle `.shell`-Breite mit `.panel-section`/`.list-row` umgestellt, analog zu Phase 1. `ParentDashboard.tsx` bleibt bei `.shell--narrow` (nur eine kurze Kinderliste), übernimmt aber ebenfalls `.panel-section`/`.list-row` statt `.card`/`.admin-row`. Auth-Formulare (Login/Registrierung/Setup-Seiten) bewusst unverändert (`.shell--narrow` + `.card` bleibt für fokussierte Einzelinhalte gültig). `.list-row` bekam dabei `flex-wrap: wrap` ergänzt (ein vorbestehendes Mobile-Overflow-Problem bei mehrteiligen Zeilen-Aktionen wie `CompanyBillingForm`).
- **Redesign-Audit: Bug-Fix — falsches "✓ Synchronisiert" ohne echten Sync, siehe Architekturplanung Abschnitt 13:** `useOfflineSync.ts` setzte den Sync-Status bei jedem Login/Reload unbedingt auf "synced", auch bei leerer Offline-Warteschlange — jetzt nur noch, wenn tatsächlich etwas synchronisiert wurde (`count > 0`), sonst `idle`.
- **Redesign-Audit: drei kleinere visuelle Inkonsistenzen behoben, siehe Architekturplanung Abschnitt 13:** `FriendCircle.tsx` — "Neuen Einladungscode erstellen" von `.btn-secondary` auf `.btn-ghost.btn-sm` heruntergestuft (passt jetzt zu "Code einlösen"), neue Unterüberschrift vor dem Einlöse-Formular ergänzt. `.progress-bar` nutzt jetzt `--surface-2` statt `--surface` als Hintergrund, damit 0-%-Balken sichtbar bleiben.
- **Redesign-Audit: mobile Lernmodus-Tableiste als horizontal scrollbarer Einzeiler, siehe Architekturplanung Abschnitt 13:** `.tab-nav` bricht 5 Tabs auf schmalen Bildschirmen nicht mehr in ein unausgeglichenes 2-2-1-Raster um, sondern scrollt ab 640px horizontal (`display:flex; overflow-x:auto`).
- **Redesign-Audit: Theorie-Sidebar nach Handlungsbereich gruppiert, sticky, Scroll-Bug behoben, siehe Architekturplanung Abschnitt 13:** `Theorie.tsx` gruppiert die Themenliste clientseitig nach `fachgebietTitle`, Nav-Titel sind per CSS auf zwei Zeilen geklemmt (voller Titel bleibt als Tooltip/Content-Überschrift erhalten), `.theory-nav` ist ab 780px sticky mit eigenem Scroll. Themenwechsel scrollt den Inhalt jetzt zuverlässig zurück nach oben (vorher ein Bug).
- **Redesign-Audit: Fortschritt-Tab in drei Sub-Tabs unterteilt ("Übersicht"/"Sozial & Erfolge"/"Einstellungen"), siehe Architekturplanung Abschnitt 13:** `Progress.tsx` nutzt jetzt dieselbe `.segmented`-ARIA-Tablist wie `Pruefungsvorbereitung.tsx` statt einer einzigen, sehr langen Seite. Alle drei Panels bleiben über `hidden` permanent gemountet.
- **Redesign Phase 1: Fullsize-Layout für die Kern-Lern-App, siehe Architekturplanung Abschnitt 13:** `.shell` von 960px auf 1160px verbreitert (entspricht der Landingpage), der bisherige pauschale `.card`-Wrapper um jeden Lernmodus in `App.tsx` ist einem schmalen `.content-narrow`-Wrapper (nur für Karteikarten/Quiz/Prüfung) gewichen — Theorie (`Theorie.tsx`, neues `.theory-layout`-Grid) und Fortschritt (`Progress.tsx`, neues `.progress-grid`) nutzen stattdessen aktiv die volle Breite. Neue generische Layout-Primitive `.panel-section`/`.list-row` ersetzen `.card`/`.admin-row` in `FriendCircle.tsx`/`Highscore.tsx`/`Lernpartner.tsx`/`Achievements.tsx`/`Progress.tsx` (Admin-/Eltern-/Unternehmens-Dashboard folgten später als Redesign Phase 2, siehe unten). Neues, erstes wiederverwendbares `src/Modal.tsx` (Portal, Fokusfalle, Escape-/Backdrop-Schließen, Scroll-Lock) für Konto-löschen (`DeleteAccount.tsx`) sowie Melden/Blockieren (`FriendCircle.tsx`). Nachtrag: die drei kleinen Einstellungs-Bereiche im Fortschritt-Tab (`Zielplanung.tsx`, `OfflineDownload.tsx`, `RedeemCompanyCode.tsx`) sitzen jetzt in einem dynamischen `.widget-grid` (CSS Grid `auto-fit`/`minmax`, dezent getönte `.widget`-Kacheln statt `.card`) statt erzwungen volle Breite zu belegen, siehe Architekturplanung Abschnitt 13.
- **F-103/F-104: Theorie-Tab entfernt, vereinheitlichter "Lernen"-Tab, siehe Architekturplanung Abschnitt 13:** Der Tab "Theorie" entfällt (`Theorie.tsx` gelöscht, `content.theorySections` bleibt im Backend unverändert bestehen, aktuell ohne Zugriffsweg). "Quiz" und "Karteikarten" verschmelzen zum Tab "Lernen" (`src/Lernen.tsx`) — je nach Präferenz (`learnFlashcardsEnabled`/`learnQuizEnabled`, neue `user`-Spalten) entweder das unveränderte `Flashcards.tsx`/`Quiz.tsx` oder die neue `src/MixedLearning.tsx` (echte gemischte Warteschlange aus fälligen Karteikarten + Quiz-Runde, inkl. Offline-Unterstützung, kein neuer Backend-Endpunkt). Neue Erstbesuch-Abfrage `src/LearningModePrompt.tsx` sowie Einstellungen-Checkboxen `src/LearningModeSettings.tsx` (vorerst im Fortschritt-Tab, F-107 verlagert das später ins Header-Benutzermenü). Neuer, vorerst leerer Platzhalter-Tab "Instrumente" (F-105).
- **F-100–F-102: Kursauswahlseite, Pflicht-Kursauswahl, Belegungs-Exklusivität, siehe Architekturplanung Abschnitt 13:** Neue `src/CourseSelection.tsx` (Suche + Kategorie-Filter, eigener `view`-Zustand `"courses"`) ersetzt die bisher direkt im Header-Dropdown eingebettete Kursliste (`CourseSwitcher.tsx` zeigt jetzt nur noch belegte Kurse + einen Link zur Auswahlseite). Ein fehlender aktiver Kurs erzwingt die Auswahlseite automatisch (F-101, kein "← Zurück"). Für Kurse der Kategorie "erwachsenenbildung" (`kurs.metadata.kategorie`) erlaubt `courses.enroll` nur eine aktive Belegung gleichzeitig (F-102) — ein Beitrittskonflikt zeigt eine Bestätigung ("Kurs wechseln?"), die die alte Belegung automatisch verlässt und die neue anlegt (atomare Transaktion). Neue, allgemeine `courses.leave`-Mutation.
- **F-107: Fortschritt-Tab restrukturiert, siehe Architekturplanung Abschnitt 13:** Neue eigenständige Haupt-Tabs "Sozial" (`src/Sozial.tsx` — FriendCircle/Highscore/Lernpartner) und "Erfolge" (`Achievements.tsx`, unverändert) lösen den bisherigen kombinierten Fortschritt-Unter-Tab ab; `Progress.tsx` zeigt nur noch die Übersicht ohne Unter-Tab-Leiste. "Einstellungen" ist jetzt ein Modal im Header-Benutzermenü (`src/SettingsModal.tsx`, analog zu `DeleteAccount.tsx`) mit denselben vier Widgets wie zuvor (Lernmodus, Zielplanung, Offline-Download, Unternehmenscode). Damit ist Iteration 7 (F-100–F-107) vollständig abgeschlossen.
- **F-51: Impressum, Datenschutzerklärung, AGB, siehe Architekturplanung Abschnitt 13:** Neue statische Seiten `src/Impressum.tsx`/`src/Datenschutzerklaerung.tsx`/`src/AGB.tsx` (Routen `/impressum`/`/datenschutz`/`/agb`, verlinkt aus dem Landing-Page-Footer und beim Registrierungsformular). Bewusst mit Platzhaltern statt echter Kontaktdaten (Kleingewerbe-Anmeldung/geschäftliche E-Mail-Adresse stehen noch aus) — als "Entwurf, noch nicht anwaltlich geprüft" gekennzeichnet, analog zu F-53. Kein Cookie-Consent-Banner nötig (nur ein technisch notwendiges Session-Cookie).
- **F-50: Feedback-Funktion für fehlerhafte Lerninhalte, siehe Architekturplanung Abschnitt 13:** Neue, wiederverwendbare `src/ReportContentButton.tsx` (Trigger-Button + Modal, analog zu `DeleteAccount.tsx`) — eingebunden in `Flashcards.tsx`, alle vier `QuizSteps.tsx`-Formate (inkl. F-104-Mischmodus, `MixedLearning.tsx`), `Exam.tsx` (Fallaufgaben) und `Fachgespraechstrainer.tsx`; bewusst NICHT im kontolosen Vorschau-Modus (`Vorschau.tsx`, F-08), gesteuert über einen neuen `canReport`-Prop auf `QuizSteps.tsx`. Moderationsansicht "Admin: Fehlermeldungen zu Lerninhalten" in `AdminPanel.tsx`, analog zu F-68.
- **F-11: Admin-/Redaktionsbereich, CMS-Teil, siehe Architekturplanung Abschnitt 13:** Neue `src/AdminContentEditor.tsx` in `AdminPanel.tsx` — Kurs-/Thema-/Typ-Filter plus Suche über die bereits geladene Liste, eigenes Formular je Content-Typ (`ContentItemForm`) statt eines rohen JSON-Editors. Lückentext-Feld nutzt die vertraute inline `___Stichwort___`-Syntax aus dem Content-Zwischenformat statt eines abstrakten Blanks-Arrays. Jede offene F-50-Fehlermeldung bekommt einen "In Redaktion bearbeiten"-Link, der den Editor direkt mit dem gemeldeten Item öffnet (`focusContentItemId`).
- **F-22: Themenbezogenes Übungsset mit frei wählbarer Fragenzahl, siehe Architekturplanung Abschnitt 13:** Neue, einklappbare `src/QuizCountControl.tsx` (10/20/30/50 Fragen) in `Quiz.tsx` und `MixedLearning.tsx` — bewusst nur eine dezente "N Fragen · Anzahl anpassen"-Steuerung statt eines verbindlichen Setup-Schritts vor jeder Runde, damit der mit F-104 reibungsarm gehaltene Rundenstart erhalten bleibt (Nutzer-Entscheidung 21.09.2026). In `MixedLearning.tsx` betrifft die Auswahl nur den Quiz-Anteil der gemischten Warteschlange, der Karteikarten-Anteil bleibt unverändert FSRS-gesteuert.
- **N-08: Basis-Logging/Monitoring inkl. Kern-KPI-Erfassung, siehe Architekturplanung Abschnitt 13:** `Quiz.tsx`/`MixedLearning.tsx` melden Start/Abschluss jeder Runde an `progress.startExerciseSet`/`completeExerciseSet` — Start feuert, sobald die Rundendaten tatsächlich geladen sind, Abschluss wird direkt in der bestehenden `next()`-Funktion erkannt (`nextIndex >= items.length`), keine zusätzliche Zustandsverwaltung nötig. Neue Stat-Kacheln "Admin: KPIs (N-08)" in `AdminPanel.tsx` (`.stat-row`/`.stat-tile`, dasselbe Vokabular wie F-93) zeigen aktive Nutzer:innen und Abschlussquote.
- **F-112: Direktes motivierendes Feedback im Quiz-Modus, siehe Architekturplanung Abschnitt 13:** Alle vier Fragetyp-Komponenten in `QuizSteps.tsx` zeigen jetzt neben dem bestehenden Sofort-Feedback eine kurze, zufällig aus vier Varianten je Ergebnis gewählte motivierende Zusatzformulierung (`pickMotivation`, einmal pro Antwort in `onSuccess` fixiert). Automatisch auch in `Quiz.tsx`, `MixedLearning.tsx` und `Vorschau.tsx` wirksam, da alle drei dieselben Komponenten nutzen. Kein Sofort-Retry, Lösung erscheint weiterhin sofort wie bisher (Nutzer-Entscheidung 21.09.2026).
- **F-111: Überarbeitete Selbsteinschätzung nach Karteikarten, siehe Architekturplanung Abschnitt 13:** Frage/Beschriftungen konkretisiert ("Wie schwierig war diese Karteikarte für dich?", Einfach/Mittel/Schwer) in `Flashcards.tsx` und `MixedLearning.tsx` (unabhängige Kopien derselben UI, wie bereits bei F-104). Neu: `ratedThisSession` (lokales Set bereits in dieser Runde bewerteter Karten-IDs) steuert, ob `Flashcards.tsx`s `review()` `progress.submitReview` oder `progress.changeReview` aufruft — erreichbar über die bestehende F-110-Zurück-Navigation, zeigt dann "Einschätzung ändern?" statt der Erstfrage. Live-Bug gefunden und behoben: "Weitere Karten laden" muss `ratedThisSession` zurücksetzen, sonst würde eine erneut fällige, namensgleiche Karte fälschlich als "Ändern" behandelt.
- **F-110: Erweiterte Karteikarten-Auswahl, siehe Architekturplanung Abschnitt 13:** `Flashcards.tsx` löst die bisherige "bei jeder Bewertung neu geladene Liste" durch eine fixierte, indexbasierte Session ab (analog zu `Quiz.tsx`) — ermöglicht Vor-/Zurück-Navigation ohne die bereits bewertete Karte zu verlieren. Neuer Picker `src/FlashcardSelection.tsx` (Checkliste über `content.themaFlashcards`, themengebunden) für die gezielte Auswahl einzelner Karten; neuer "Nur schwierige Karten"-Filter samt Stern-Umschalter (`StarIcon`, `progress.toggleDifficultyFlag`) — rein additiv zur FSRS-Selbsteinschätzung. Neue `src/FlashcardStartSideSettings.tsx` im Einstellungen-Modal für die dauerhafte "Antwortseite zuerst"-Präferenz. Bewusst nur online, der Offline-Pfad (F-42) bleibt unverändert.
- **F-109: Modul-/Fachgebiets-Übersicht, siehe Architekturplanung Abschnitt 13:** `Progress.tsx`s bestehende "Fortschritt je Fachgebiet"-Ansicht ist jetzt die explizite Navigationsebene, die F-109 verlangt — jeder Themen-Fortschrittsblock ist ein klickbarer `<button>`, der über den bestehenden F-27-Themenfilter (wie bei F-14/F-50) gezielt in den "Lernen"-Tab springt. Neuer "Standort-Hinweis": Ist ein Thema gerade per F-27-Filter aktiv, wird sein Block hervorgehoben (`--sprout-deep`, Zusatzlabel "· aktuell ausgewählt"). Der Fachgebiets-Summenblock bleibt bewusst nicht klickbar. Rein frontend-seitig, keine Backend-Änderung.
- **F-108: Personalisierter Wiedereinstieg, siehe Architekturplanung Abschnitt 13:** Neues, optionales "Anzeigename"-Feld im Registrierungsformular sowie in einer neuen `DisplayNameSettings.tsx`-Widget im Einstellungen-Modal (leerer String löscht ihn wieder). `App.tsx` zeigt eine namentliche oder neutrale Begrüßung ("welcome-greeting") sowie eine "Weiter, wo du aufgehört hast:"-Beschriftung oberhalb der bestehenden, unveränderten F-27-Vorschlagsliste — auf Themen-Ebene statt exakter Positions-Wiederherstellung (Nutzer-Entscheidung 21.09.2026).
- **F-14: Volltextsuche über alle Lerninhalte, siehe Architekturplanung Abschnitt 13:** Neue `src/Suche.tsx` im bisherigen Platzhalter-Tab "Instrumente" — Suchfeld + Trefferliste (`content.search`, ILIKE über `prompt`/`explanation`, ohne `theorie`-Treffer). "Zu diesem Thema lernen" verlinkt über den bestehenden F-27-Themenfilter in den "Lernen"-Tab statt einer neuen Detailansicht. Beachte bei künftigen bedingt aktivierten (`enabled: false`) react-query-v4-Queries: `isLoading` bleibt bei fehlenden Daten dauerhaft `true`, auch wenn die Query nie läuft — für Lade-Anzeigen zusätzlich `isFetching` und/oder die Aktivierungsbedingung prüfen (hier live als Bug gefunden und behoben).
- **F-01: E-Mail-Verifizierung bei Registrierung, siehe Architekturplanung Abschnitt 13:** Neues, dauerhaft sichtbares `src/EmailVerificationBanner.tsx` (oberhalb des Hauptinhalts in allen eingeloggten Ansichten) erinnert unverifizierte Konten an die Bestätigung, mit "Erneut senden"-Button — bewusst als weiches Gate, blockiert den Login nicht. Neue Zielseite `src/VerifyEmail.tsx` unter `/verify-email` (analog zu `ConsentConfirm.tsx`/F-08). Nur volljährige Konten sehen das Banner/bekommen eine Verifizierungsmail.
- **Noch offen:** granulare Kind-Berechtigungen im Eltern-Dashboard (F-90, setzt Gamification F-66 voraus), restliches Sozial-Modul (F-61/F-64/F-65), echte Kontaktdaten im Impressum/in der Datenschutzerklärung (sobald Kleingewerbe-Anmeldung abgeschlossen ist).

## Entwicklung

```bash
pnpm --filter @edukedo/api dev   # Backend auf Port 3001 (siehe apps/api/README.md)
pnpm --filter @edukedo/web dev   # Frontend auf Port 5173
```
