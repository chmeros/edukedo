{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "noEmit": true,
    "types": ["vite/client"]
  },
  "include": ["src"],
  // F-43: sw.ts läuft unter "webworker"-statt "dom"-Lib (siehe tsconfig.sw.json) — beide
  // schließen sich gegenseitig aus, deshalb hier ausgeschlossen.
  "exclude": ["src/sw.ts"]
}
