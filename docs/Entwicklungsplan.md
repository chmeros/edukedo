# Entwicklungsplan: edukedo

Stand: 18.09.2026 · Grundlage: Anforderungskatalog Version 0.24 (insbesondere Abschnitt 9, Phasenplanung) und Architekturplanung Version 0.8 (insbesondere „Nächste Schritte")

> **Aktualisierung 18.09.2026:** Neue Anforderungsgruppe „Navigation, Kursauswahl & In-App-Struktur" (F-100–F-107, Anforderungskatalog Abschnitt 5.13) als neue **Iteration 7** ergänzt. Anders als die bisherigen Iterationen ist das kein neuer, additiver Funktionsbereich, sondern ein Umbau bereits bestehender, produktiver UI (Header-Dropdown, Theorie-Tab, Quiz-/Karteikarten-Tabs, Fortschritt-Tab-Struktur, Mehrfach-Kursbelegung) — deshalb bewusst als eigene Iteration statt rückwirkender Änderung an den bereits als erledigt markierten Aufgaben in Iteration 1/3/5, um deren historischen Umsetzungsstand nicht zu verfälschen. Die beiden zunächst offenen Namensfragen sind noch am selben Tag entschieden worden: Der vereinheitlichte Lernmodus-Tab (F-104) heißt **„Lernen"**, Theorie-Inhalte (F-103) bleiben vorerst ohne jeglichen Zugriffsweg unsichtbar.
>
> **Aktualisierung 14.09.2026 (Mathe-Content vorab erstellt):** Der Lerncontent für den Mathematik-Kurs (Klasse 9) wurde bereits jetzt vollständig für alle drei Themenblöcke erstellt (Algebra & Funktionen, Geometrie, Stochastik — siehe Anforderungskatalog Abschnitt 4, 9), nicht erst gestaffelt wie in Iteration 3/5 unten vorgesehen. Die **Programmier-Reihenfolge und das Validierungs-Gate selbst ändern sich dadurch nicht** — Iteration 3 aktiviert weiterhin nur den Mathe-Kurs mit `is_published = false → true`, Iteration 4/5 prüfen weiterhin anhand echter KPIs, ob sich der Ausbau lohnt. Der Unterschied: Wenn das Gate positiv ausfällt, liegt der Content für den weiteren Ausbau (Iteration 5) bereits vollständig vor, statt erst dann erstellt werden zu müssen. Die zugehörigen Content-Tasks in Iteration 0, 3 und 5 sind unten entsprechend als bereits erledigt bzw. angepasst markiert. Zusätzlich wurde die in Iteration 0 vorgesehene Gegenprüfung des Themenkatalogs mit echtem Schulbuchmaterial **nicht** vorgezogen durchgeführt — der Katalog bleibt vorläufig (siehe Anforderungskatalog Abschnitt 4, 7).
>
> **Aktualisierung 14.09.2026:** Business-Lizenzen & Sponsoring (F-91–F-94, siehe Anforderungskatalog Abschnitt 5.12 und Architekturplanung Abschnitt 4.5/13) als neue Aufgaben in **Iteration 6** ergänzt — passend zur dortigen Phase-4-Einordnung. Bewusst unter „Programmierung (Kern)" statt „Programmierung (Payment)" einsortiert, weil die Lizenz laut Architekturentscheidung keine Premium-Freischaltung auslöst und deshalb ohne Anbindung an den separaten Payment-Service auskommt — diese Aufgaben sind unabhängig von den übrigen Iteration-6-Payment-Aufgaben umsetzbar. Ergänzend: ein Recht-&-Compliance-Task zu Unternehmens-AGB sowie zwei Testing-Tasks zur Zugriffssperre bei aggregierter Statistik.
>
> **Aktualisierung 12.09.2026 (Entwickler-Review):** Vier Anpassungen gegenüber der ersten Fassung: (1) **Iteration 2 und 3 wurden dependency-bewusst neu geschnitten** — der Eltern-Consent-Flow (F-08/F-90) steht jetzt bewusst *vor* der Live-Schaltung des Mathe-Kurses, weil die Architekturplanung genau das verlangt (Consent-Infrastruktur muss stehen, bevor ein überwiegend von Minderjährigen genutzter Kurs veröffentlicht wird). (2) Das **`REPORT`/`BLOCK`-Datenmodell wandert von Iteration 3 nach Iteration 0**, weil es ohnehin nur Schema ohne UI ist und laut Architekturplanung in dieselbe erste Migrations-Charge gehört. (3) **Konto-Selbstlöschung (F-06)** ist jetzt ein expliziter Task in Iteration 1, statt implizit „irgendwann" mitzulaufen — ab dieser Iteration entstehen echte personenbezogene Daten. (4) Iteration 0 berücksichtigt jetzt auch **`CONTENT_ITEM_VERSION`, `TAG`/`CONTENT_ITEM_TAG`** sowie die dafür nötigen Postgres-Extensions, passend zum inzwischen vertieften Datenmodell (Architekturplanung Version 0.5/0.6).

## Grundprinzip

Der Plan ist **iterativ** organisiert, nicht kalendarisch: Jede Iteration liefert einen abgeschlossenen, für sich sinnvollen Zuwachs (idealerweise einen lauffähigen vertikalen Slice), bevor die nächste beginnt. Es gibt bewusst keine Zeitangaben je Iteration oder Aufgabe — nur den bereits an anderer Stelle festgehaltenen groben Rahmen für den MVP-Launch insgesamt (Q2 2027, siehe Anforderungskatalog Abschnitt 1). Reihenfolge und Detailtiefe der Aufgaben können sich beim Arbeiten noch verschieben; der Plan ist als lebendes Dokument gedacht, das mit dem Projektfortschritt aktualisiert wird — Aufgaben können als erledigt abgehakt werden (`- [x]`).

Jede Iteration ist nach **Bereichen** aufgeschlüsselt: Programmierung (Kern), Programmierung (Payment, erst ab Iteration 6 relevant), Content, Recht & Compliance, Organisatorisches, Testing, Nutzer:innen-Feedback. Nicht jeder Bereich hat in jeder Iteration Aufgaben.

## Iteration 0 — Grundgerüst & Vorbereitung

Ziel: Alles ist bereit, um mit dem ersten inhaltlichen Slice (Iteration 1) zu beginnen — noch ohne sichtbaren Lernfortschritt für Nutzer:innen.

**Programmierung (Kern)**
- [x] Monorepo aufsetzen (pnpm Workspaces + Turborepo), Grundstruktur `/apps/web`, `/apps/api`, `/packages/shared` anlegen
- [x] CI-Grundgerüst (GitHub Actions: Lint, Typecheck, Tests, Build bei PR) — Lint/Typecheck/Test/Build laufen jetzt tatsächlich gegen echten Code statt gegen TODO-Platzhalter
- [x] Drizzle-Migrationsverwaltung einrichten, benötigte Extensions aktivieren (`citext` für `"user".email`/`parent.email`, `pgcrypto` für `gen_random_uuid()`) — erste Migration `0000_enable_extensions.sql`; lokale Postgres/Redis-Instanz über `docker-compose.yml` im Repo-Root (siehe Architekturplanung Abschnitt 13)
- [ ] Verwaltete PostgreSQL-Cloud-Instanz (z. B. Neon/Supabase) für Staging/Produktion tatsächlich anlegen — Kontoerstellung/Vertragsabschluss, kein Code-Task; die Migrationen selbst sind bereits lauffähig vorbereitet
- [x] Generisches Content-Datenmodell migriert: `kurs`, `fachgebiet`, `thema`, `content_item`, `content_item_version`, `answer_option` (inkl. `side`-Feld für Zuordnungs-Paare), `tag`, `content_item_tag`, `user_course` (F-09, F-13) — siehe `apps/api/src/db/schema.ts` + `apps/api/drizzle/0001_*.sql`
- [x] `report`/`block`-Datenmodell (F-68) direkt mit den übrigen Migrationen angelegt, auch ohne UI (vorgezogen aus der ursprünglich späteren Jugendschutz-Iteration, siehe Architekturplanung Abschnitt 4.4)
- [x] Auth-Grundgerüst (Eigenbau): Argon2id-Hashing, Sessions/Cookies nach dem Lucia-Pattern, Rollenmodell — `user.role` (`learner`/`content_editor`/`admin`) plus eigener `parent`-Account-Typ mit eigener Session (siehe Architekturplanung Abschnitt 13); inkl. lauffähiger `register`/`login`/`logout`/`me`-Endpunkte (nimmt das Iteration-1-Item "Registrierung/Login inkl. Altersabfrage" unten vorweg)
- [x] tRPC-Router-Grundstruktur für die Kern-API (`health`, `auth`; weitere Module folgen modulweise)

**Content**
- [x] Zwischenformat für Content-Erstellung festlegen (z. B. Markdown-Dateien mit definiertem Frontmatter oder eine Tabellenvorlage) — unabhängig vom Entwicklungsstand des Redaktionssystems (F-11/F-17). **Erledigt 14.09.2026**, siehe `content/README.md` im Repo.
- [x] Eigene Wissensquellen für HB3 (Führen/Verwalten/Ausbilden) sichten und mit Abschnitt 7 (Urheberrecht: frei formuliert, keine 1:1-Übernahme) abgleichen. **Erledigt 14.09.2026** auf Basis des vom Nutzer bereitgestellten DIHK-Rahmenplans, siehe Anforderungskatalog Abschnitt 4.
- [ ] Mathematik-Themenkatalog (Abschnitt 4) mit konkretem Schulbuch-/Übungsmaterial für Klasse 9 gegenprüfen. **Hinweis (14.09.2026):** Diese Gegenprüfung wurde bewusst **nicht** vor der Content-Erstellung durchgeführt — der Mathe-Content (siehe unten, Iteration 3/5) entstand auf Basis des unverifizierten, vorläufigen Katalogs. Diese Aufgabe bleibt offen und sollte vor Veröffentlichung des Kurses für echte Lernende nachgeholt werden.

**Recht & Compliance**
- [ ] Kleingewerbe anmelden (Einzelunternehmen), Steuernummer/ladungsfähige Anschrift für spätere Impressumspflicht (F-51) einholen
- [ ] Kanzlei-Shortlist für die spätere Jugendschutzprüfung erstellen (unverbindlich, siehe Abschnitt 7)

**Organisatorisches**
- [ ] Domains edukedo.com/.de mit Hosting/DNS verbinden, geschäftliche E-Mail-Adresse einrichten
- [ ] Hosting-Accounts für Frontend (Vercel/Cloudflare Pages) und Kern-Backend (Railway/Render/Fly.io) anlegen

**Testing**
- [x] Testinfrastruktur aufsetzen (Vitest, Testcontainers für Postgres) — `apps/api/test/db.integration.test.ts` läuft Migrationen gegen einen echten, per Testcontainers gestarteten Postgres-Container; benötigt lokal einen laufenden Docker-Daemon

## Iteration 1 — Erster vertikaler Slice: Fachwirt-Pilot, HB3 (erstes Thema)

Ziel: Eine Person kann sich registrieren und mit echtem, selbst erstelltem Content zu einem ersten Thema aus HB3 lernen — Karteikarten und Quiz funktionieren Ende-zu-Ende.

**Programmierung (Kern)**
- [x] Registrierung/Login inkl. Altersabfrage (F-01, F-02); `USER.birth_date`/`is_minor` werden gesetzt, volle Jugendschutz-Durchsetzung (F-08) ist hier noch nicht nötig (Fachwirt-Pilot = erwachsene Zielgruppe) — bereits mit dem Auth-Grundgerüst in Iteration 0 umgesetzt (`apps/api/src/trpc/routers/auth.ts`). **Der E-Mail-Verifizierungsteil von F-01 fehlte dabei zunächst** (`user.email_verified_at` existierte im Schema, wurde aber nie gesetzt) — siehe eigener Nachtrag weiter unten, ergänzt am 19.09.2026
- [x] Konto-Selbstlöschung (F-06) implementieren: Eigenes Konto inkl. kaskadierender Daten vollständig löschen können — `auth.deleteAccount` (Passwort-Bestätigung, dann ein einzelnes `DELETE FROM user`, den Rest übernehmen die bereits im Datenmodell festgelegten `ON DELETE CASCADE`/`SET NULL`-Regeln) plus Frontend-Bestätigungsdialog (`apps/web/src/DeleteAccount.tsx`). Per Testcontainers-Integrationstest und live gegen echtes Postgres verifiziert (inkl. Kurs-/Fortschritts-/Session-Daten sowie des asymmetrischen `report`-Löschverhaltens)
- [x] Karteikarten-Modus (F-20) mit FSRS-Algorithmus (`ts-fsrs`, `USER_PROGRESS`-Felder `difficulty`/`stability`/`state`) — Backend (`apps/api/src/fsrs/scheduler.ts`, Router `content`/`progress`) und Frontend (`apps/web/src/Flashcards.tsx`) Ende-zu-Ende gegen echtes Postgres verifiziert; lief zunächst nur mit technischem Platzhalter-Content (`db:seed`), seit dem HB3-Content-Import (siehe unten) auch mit echtem Fachwirt-Content. Dafür minimal auch `courses.list`/`courses.enroll` (F-09) ergänzt, da der Karteikarten-Modus eine Kurseinschreibung voraussetzt — die vollständige Kursauswahl-UI folgt trotzdem erst in Iteration 3
- [x] **Theorie-Ansicht ergänzt (nicht ursprünglich als eigener Punkt geplant, aber Voraussetzung für den HB3-Content-Import):** `content.theorySections` (Backend) und `apps/web/src/Theorie.tsx` (neuer Tab) zeigen die Theorie-Fließtexte je Thema — vorher gab es für `content_item.type = "theorie"` keine Leseansicht
- [x] **Kurzantwort-Quiz-Format ergänzt (F-21, vierter Fragetyp, nicht ursprünglich Teil dieses Slice):** `quiz.submitKurzantwort` (Backend) und `apps/web/src/Quiz.tsx` — freies Textfeld, serverseitige Prüfung case-insensitive nach Trim (`kurzantwortPayloadSchema.match_mode`). Ergänzt, weil der echte HB3-Content Kurzantwort-Fragen enthält
- [x] Quiz-Modus (F-21) mit Sofort-Feedback — alle drei Formate (Multiple Choice, Zuordnung, Lückentext) umgesetzt (Backend `apps/api/src/trpc/routers/quiz.ts`, Frontend `apps/web/src/Quiz.tsx`) und Ende-zu-Ende gegen echtes Postgres verifiziert; richtige Antwort/Zuordnung/Lösung wird jeweils serverseitig erst bei der Auswertung offengelegt, nie beim Laden der Fragen
- [x] Einfache Fortschrittsanzeige je Fachgebiet und Thema (F-30) — Backend (`progress.overview`) und Frontend (`apps/web/src/Progress.tsx`) umgesetzt und gegen echtes Postgres verifiziert; "beherrscht" = FSRS-Zustand `review` (siehe Architekturplanung Abschnitt 13). Umfasst aktuell nur Karteikarten, da nur der Karteikarten-Modus `user_progress` schreibt
- [x] PWA-Grundgerüst (F-40, F-41): Service Worker, installierbar — `vite-plugin-pwa` (App-Shell-Precaching + Web-App-Manifest), Layout gegen 375px-Mobile-Breakpoint live verifiziert. Volle Offline-Synchronisierung (F-42, IndexedDB-Warteschlange) ist bewusst nicht Teil dieses Schritts. Platzhalter-Icons (kein echtes Branding vorhanden), siehe Architekturplanung Abschnitt 13
- [x] **Impressum, Datenschutzerklärung, AGB (F-51) — ursprünglich kein eigener Task, beim Anforderungskatalog-Audit vom 18.09.2026 als Lücke entdeckt (Muss-Anforderung ohne jede Umsetzung).** **Erledigt 18.09.2026:** drei neue statische Seiten (`Impressum.tsx`/`Datenschutzerklaerung.tsx`/`AGB.tsx`, Routen `/impressum`/`/datenschutz`/`/agb`), verlinkt aus dem Landing-Page-Footer und beim Registrierungsformular. Bewusst mit Platzhaltern statt echter Kontaktdaten (Nutzer-Entscheidung), da Kleingewerbe-Anmeldung/geschäftliche E-Mail-Adresse laut Iteration 0 noch ausstehen — als "Entwurf, noch nicht anwaltlich geprüft" gekennzeichnet, analog zu F-53. Cookie-Hinweis in der Datenschutzerklärung erklärt, warum kein Consent-Banner nötig ist (nur ein technisch notwendiges Session-Cookie). Siehe Architekturplanung Abschnitt 13.
- [x] **Feedback-Funktion für fehlerhafte Lerninhalte (F-50) — ursprünglich kein eigener Task, beim Anforderungskatalog-Audit vom 18.09.2026 als Lücke entdeckt (Soll-Anforderung ohne jede Umsetzung, nicht zu verwechseln mit der Melde-/Blockierfunktion F-68 gegen andere Nutzer:innen).** **Erledigt 19.09.2026:** neue `content_report`-Tabelle (analog zu `report`/F-68, aber content-item- statt nutzerbezogen), neuer Router `contentFeedback.report`, Moderationsansicht unter `admin.contentReports`/`admin.resolveContentReport`. Neue, wiederverwendbare `ReportContentButton.tsx`-Komponente in `Flashcards.tsx`, allen vier `QuizSteps.tsx`-Formaten (inkl. F-104-Mischmodus über `MixedLearning.tsx`), `Exam.tsx` (Fallaufgaben) und `Fachgespraechstrainer.tsx` eingebunden — bewusst NICHT in `Vorschau.tsx` (F-08, kontoloser Modus ohne Datenbank-Schreibzugriff), gesteuert über einen neuen `canReport`-Prop. Siehe Architekturplanung Abschnitt 13.
- [x] **Volltextsuche über alle Lerninhalte (F-14) — ursprünglich kein eigener Task, beim Anforderungskatalog-Audit vom 18.09.2026 als Lücke entdeckt (Soll-Anforderung ohne jede Umsetzung).** **Erledigt 19.09.2026:** neue `content.search`-Query (ILIKE über `prompt`/`explanation`, mit `escapeLikePattern`-Escaping gegen `%`/`_` in Suchbegriffen, ohne `theorie`-Treffer), neue `Suche.tsx`-Komponente im bisherigen Platzhalter-Tab "Instrumente". Ergebnisse verlinken über den bestehenden F-27-Themenfilter in den "Lernen"-Tab statt einer neuen Detailansicht. Live-Bug gefunden und behoben (react-query-v4-`isLoading` bei deaktivierter Query zeigte die Lade-Anzeige fälschlich im leeren Ausgangszustand). Siehe Architekturplanung Abschnitt 13.
- [x] **E-Mail-Verifizierung bei Registrierung (F-01) — beim zweiten Anforderungskatalog-Audit vom 19.09.2026 als Lücke entdeckt (Muss-Anforderung, nur teilweise umgesetzt: Registrierung/Login/Altersabfrage bereits seit Iteration 0 fertig, die eigentliche Verifizierung fehlte).** **Erledigt 19.09.2026:** neue `email_verification_token`-Tabelle (analog zu `consent_token`/F-08), `auth.verifyEmail`/`auth.resendVerificationEmail`, neue Zielseite `VerifyEmail.tsx` unter `/verify-email`. Bewusst als weiches Gate (Nutzer-Entscheidung): blockiert den Login nicht, sondern zeigt ein dauerhaftes Hinweis-Banner (`EmailVerificationBanner.tsx`) bis zur Bestätigung; nur volljährige Konten verschicken eine eigene Verifizierungsmail (Minderjährige haben mit F-08 bereits einen bestätigten Eltern-E-Mail-Kanal). Siehe Architekturplanung Abschnitt 13.
- [x] **Rate-Limiting bei Login (N-02) — beim zweiten Anforderungskatalog-Audit vom 19.09.2026 als Lücke entdeckt (das `auth/rate-limit.ts`-Modul aus F-63 existierte bereits, war aber nicht an `auth.login` angebunden).** **Erledigt 19.09.2026:** `auth.login` prüft vor der Passwortprüfung ein In-Memory-Rate-Limit je E-Mail-Adresse (10 Versuche/15 Minuten, `checkRateLimit`, dasselbe Muster wie bei F-63) — blockiert danach auch bei zufällig korrektem Passwort. Siehe Architekturplanung Abschnitt 13 für den bewusst in Kauf genommenen Trade-off (E-Mail- statt IP-Schlüsselung).
- [x] **Admin-/Redaktionsbereich, CMS-Teil (F-11) — beim zweiten Anforderungskatalog-Audit vom 19.09.2026 als Lücke entdeckt (Muss-Anforderung, nur die Grundausbaustufe aus Iteration 3 war fertig: Kurs-Veröffentlichung, Content-Import-Trigger, Moderationsansichten).** **Erledigt 19.09.2026:** Neuer Router `adminContent.ts` (Pflege UND — auf ausdrücklichen Nutzerwunsch, über die ursprüngliche Empfehlung hinaus — Neuanlage einzelner Content-Items), eigenes Formular je der acht `content_item.type`-Werte statt eines rohen JSON-Editors. Lückentext-Editor nutzt dieselbe inline `___Stichwort___`-Syntax wie das Content-Zwischenformat (`parseLueckentext`/`renderLueckentextSource`, aus `content-parser.ts`/`content-serializer.ts` als wiederverwendbare Funktionen extrahiert). Schließt den Kreis zu F-50: jede offene Fehlermeldung bekommt einen "In Redaktion bearbeiten"-Link direkt zum gemeldeten Item. Nebenbefund als separate Aufgabe vorgemerkt: F-14-Suchergebnisse zeigen bei Lückentext-Items derzeit die Antwort im Klartext (Ursache liegt in F-14, nicht in F-11). Siehe Architekturplanung Abschnitt 13.
- [x] **Themenbezogenes Übungsset mit frei wählbarer Fragenzahl (F-22) — beim ersten Anforderungskatalog-Audit vom 19.09.2026 als Lücke entdeckt (Muss-Anforderung; `quiz.quizItems` hatte die Rundengröße fest auf 20 verdrahtet).** **Erledigt 21.09.2026:** Neues `quizItemsInputSchema` (`count`, 1–50, Default 20) statt Erweiterung des von `content.dueCards` mitgenutzten `themaFilterableKursInputSchema`. Neue, einklappbare `QuizCountControl.tsx` (10/20/30/50) in `Quiz.tsx` und `MixedLearning.tsx` — bewusst als optionale Auswahl statt eines verbindlichen Setup-Schritts, damit der mit F-104 reibungsarm gehaltene Rundenstart erhalten bleibt. In `MixedLearning.tsx` steuert `count` nur den Quiz-Anteil, der Karteikarten-Anteil bleibt FSRS-gesteuert. Offline-Pfad (`loadOfflineQuizRound`, F-42) symmetrisch mit demselben Parameter nachgezogen. Siehe Architekturplanung Abschnitt 13.
- [x] **Basis-Logging/Monitoring inkl. Kern-KPI-Erfassung (N-08) — beim ersten Anforderungskatalog-Audit vom 19.09.2026 als Lücke entdeckt (`app.ts` nutzte nur Fastifys Standardlogging, keinerlei KPI-Erfassung).** **Erledigt 21.09.2026:** Neue `exercise_set`-Tabelle trackt Start/Abschluss jeder Quiz-/Mischmodus-Runde (F-22/F-104) für die "Abschlussquote von Übungssets"-KPI (Anforderungskatalog Abschnitt 11) — Karteikarten-Sitzungen (F-20, endlos, keine Zielgröße) zählen bewusst nicht als Übungsset. Tracking in `Quiz.tsx`/`MixedLearning.tsx` verankert (Start bei Rundenladen, Abschluss in der bestehenden `next()`-Funktion), nur online erfasst. Neuer `onError`-Hook am `fastifyTRPCPlugin` loggt gezielt `INTERNAL_SERVER_ERROR` (nicht jede erwartete 4xx-Antwort), ohne `input` (Passwort-Leak-Schutz). Neue `admin.kpis`-Query zeigt aktive Nutzer:innen (WAU, 7 Tage) und Abschlussquote (30 Tage) als Stat-Kacheln im Admin-Panel. Siehe Architekturplanung Abschnitt 13.
- [x] **Personalisierter Wiedereinstieg (F-108) — aus dem Nutzer-Feedback vom 18.09.2026 (Version 0.25 des Anforderungskatalogs, erweitert F-27).** **Erledigt 21.09.2026:** Neues, rein optionales `display_name`-Feld auf `user` (settbar bei Registrierung, änderbar über neue `DisplayNameSettings.tsx` im Einstellungen-Modal) treibt eine namentliche Begrüßung ("Hallo, {Name}!") statt einer neutralen ("Schön, dass du wieder da bist!") auf der Startseite. "Weiter, wo du aufgehört hast" nutzt bewusst den bestehenden F-27-Vorschlagsmechanismus auf Themen-Ebene (Nutzer-Entscheidung), keine exakte Frage-/Positions-Wiederherstellung — kein neuer Backend-Endpunkt, nur eine zusätzliche Beschriftung oberhalb der bestehenden Vorschlagsliste. Siehe Architekturplanung Abschnitt 13.
- [x] **Modul-/Fachgebiets-Übersicht (F-109) — aus dem Nutzer-Feedback vom 18.09.2026 (Version 0.25 des Anforderungskatalogs, ergänzt F-30).** **Erledigt 21.09.2026:** Die bestehende "Fortschritt je Fachgebiet"-Ansicht (`Progress.tsx`, Daten aus `progress.overview`) erfüllte bereits fast alle Anforderungen — ihr fehlte nur die "explizite Navigationsebene". Jeder Themen-Fortschrittsblock ist jetzt ein klickbarer `<button>`, der über denselben F-27-Themenfilter (wie bei F-14/F-50) gezielt in den "Lernen"-Tab springt; der Fachgebiets-Summenblock bleibt bewusst nicht klickbar. Neuer "Standort-Hinweis": Das aktuell per F-27-Filter aktive Thema wird optisch hervorgehoben ("· aktuell ausgewählt"). Kein neuer Backend-Endpunkt, keine Schemaänderung — rein frontend-seitig. Siehe Architekturplanung Abschnitt 13.
- [x] **Erweiterte Karteikarten-Auswahl (F-110) — aus dem Nutzer-Feedback vom 18.09.2026 (Version 0.25 des Anforderungskatalogs, erweitert F-22).** **Erledigt 21.09.2026:** Vier Teile — gezielte Auswahl einzelner Karten (neuer Picker `FlashcardSelection.tsx` über `content.themaFlashcards`, themengebunden statt kursweit), freie Wahl der Startseite (dauerhafte Präferenz `user.flashcard_start_with_answer`, Einstellungen-Modal), Vor-/Zurück-Navigation innerhalb der Runde (löst die bisherige "bei jeder Bewertung neu geladene Liste" durch eine fixierte, indexbasierte Session ab, analog zu `Quiz.tsx`) sowie eine manuelle "schwierig"-Markierung (`user_progress.flagged_as_difficult`). Die im Anforderungskatalog offen gelassene Detailfrage zum Verhältnis der "schwierig"-Markierung zur FSRS-Selbsteinschätzung wurde per Rückfrage geklärt: rein additives Flag, FSRS-Zustand bleibt unberührt. `content.dueCards` um zwei sich gegenseitig ausschließende Filter erweitert (`contentItemIds`/`onlyFlagged`), beide unabhängig von der regulären FSRS-Fälligkeit. Bewusst nur online (Offline-Pfad F-42 unverändert). Siehe Architekturplanung Abschnitt 13.
- [x] **Überarbeitete Selbsteinschätzung nach Karteikarten (F-111) — aus dem Nutzer-Feedback vom 18.09.2026 (Version 0.25 des Anforderungskatalogs, erweitert F-20).** **Erledigt 21.09.2026:** Frage/Beschriftungen konkretisiert ("Wie schwierig war diese Karteikarte für dich?", Einfach/Mittel/Schwer statt Gut/Schwer/Nochmal — dieselben drei FSRS-Grade, nur umbenannt) in `Flashcards.tsx` und `MixedLearning.tsx`. Der geforderte unabhängige Wiederaufruf einer Karte war durch F-110 bereits abgedeckt. Neu: nachträgliches Ändern einer Bewertung — die im Anforderungskatalog offen gelassene Frage, ob das auf den FSRS-Zustand zurückwirkt, wurde per Rückfrage geklärt: "echtes Rückgängig". Neues `user_progress.previous_snapshot`-Feld hält den FSRS-Zustand vor der jeweils letzten Bewertung fest; neue Mutation `progress.changeReview` rechnet beim Ändern immer davon neu (bleibt bei mehrfachem Ändern idempotent) und korrigiert den zuletzt erfassten `learning_event` statt einen zweiten anzuhängen (verfälscht sonst F-31/F-32). Erreichbar über die F-110-Zurück-Navigation, nur online. Siehe Architekturplanung Abschnitt 13.
- [x] **Direktes motivierendes Feedback im Quiz-Modus (F-112) — aus dem Nutzer-Feedback vom 18.09.2026 (Version 0.25 des Anforderungskatalogs, erweitert F-21).** **Erledigt 21.09.2026:** Kurze, zufällig gewählte motivierende Zusatzformulierung (vier Varianten je Ergebnis) neben dem bestehenden Sofort-Feedback in allen vier Fragetyp-Komponenten (`QuizSteps.tsx`, damit automatisch auch in `Quiz.tsx`/`MixedLearning.tsx`/`Vorschau.tsx`). Die im Anforderungskatalog offen gelassenen Fragen (Sofort-Retry, Zeitpunkt der Lösungsanzeige, Demotivierungsvermeidung) wurden per Rückfrage geklärt: kein Sofort-Retry, Lösung erscheint weiterhin sofort wie bisher, Wiederholung falscher Antworten bleibt Sache des bestehenden F-26-Wiederholungssets — Demotivierung wird durch die variierenden, milde formulierten Texte abgefedert statt durch ein technisches Retry-System. Rein frontend-seitig, keine Backend-Änderung. Siehe Architekturplanung Abschnitt 13.
- [x] **Neue Quiz-Formate: Wahr/Falsch, Entweder-Oder, Was passt nicht dazu (F-113, Teil 1 von 2) — aus dem Nutzer-Feedback vom 18.09.2026 (Version 0.25 des Anforderungskatalogs, erweitert F-21).** **Erledigt 21.09.2026:** Drei der vier neu geforderten Fragetypen — strukturell identisch zu Multiple Choice (answer_option-basiert, genau eine Option richtig), daher über einen gemeinsamen `MC_LIKE_QUIZ_TYPES`-Typ statt dreifacher Code-Duplizierung umgesetzt (siehe Architekturplanung Abschnitt 13). Neue Admin-Formulare (`AdminContentEditor.tsx`, Nutzer-Entscheidung: Authoring jetzt statt später), neue `TwoChoiceStep`-Komponente für Wahr/Falsch und Entweder-Oder (zwei große Antwortflächen statt vertikaler Liste), Was-passt-nicht-dazu nutzt die bestehende `MultipleChoiceStep`-Komponente unverändert. Bewusst nur online. **Noch offen (Teil 2 von 2):** "Sortieren" (Drag-and-Drop-Reihenfolge) — braucht echte neue Datenstruktur (Positionsangabe je Option) und eine eigene Drag-and-Drop-Komponente, per Rückfrage bewusst als eigener, separater Schritt vorgemerkt. Bulk-Import-/-Export-Unterstützung (content-parser.ts/content-serializer.ts) für die drei neuen Typen ebenfalls noch offen, falls künftig ein größerer Content-Bestand im Zwischenformat benötigt wird.
- [x] **Interaktive Diagramme und Modelle: SWOT-Matrix, Balanced Scorecard, Ansoff-Matrix (F-114, Teil 1 von 2) — aus dem Nutzer-Feedback vom 18.09.2026 (Version 0.25 des Anforderungskatalogs, erweitert F-21/Zuordnung).** **Erledigt 21.09.2026:** Drei der vier geforderten Modelle — alle strukturell "4 feste Zonen, Begriffe per Drag-and-Drop hineinziehen", daher über einen gemeinsamen `QUADRANT_QUIZ_TYPES`-Typ statt dreifacher Duplizierung umgesetzt (siehe Architekturplanung Abschnitt 13). Erste neue Frontend-Produktionsabhängigkeit des Projekts: `@dnd-kit/core` für zuverlässiges Maus-/Touch-Drag-and-Drop (Nutzer-Entscheidung nach Rückfrage, statt einer selbst gebauten Pointer-Events-Lösung). Neue Admin-Formulare mit Begriff+Zonen-Dropdown je Zeile, neue `QuadrantStep`-Komponente (Lernenden-UI). Zonen sind fest im Code hinterlegt (`QUADRANT_MODELS`), nicht content-autorierbar. Beliebig oft umsortieren vor Prüfung, danach einmalige Auswertung mit farblicher Hervorhebung — wie die bestehende Zuordnungsfrage. Bewusst nur online. **Noch offen (Teil 2 von 2):** Gantt-Diagramm (Zeitleiste/Phasenfolge statt Quadranten, eigene Datenstruktur und eigenes Layout nötig) sowie Bulk-Import-/-Export-Unterstützung für die drei neuen Typen, analog zu F-113.
- [x] **Wortauswahl-Lückentext (F-115) — aus dem Nutzer-Feedback vom 18.09.2026 (Version 0.25 des Anforderungskatalogs, erweitert F-21/Lückentext).** **Erledigt 22.09.2026:** Neuer, eigenständiger Content-Typ `luecken_auswahl` — Begriffe per Drag-and-Drop aus einem Wortpool (richtige Begriffe + mindestens 2 Distraktoren, `LUECKEN_AUSWAHL_MIN_DISTRACTORS`) statt Freitext-Eingabe. Die im Anforderungskatalog offen gelassenen Fragen (Mindestanzahl Zusatzbegriffe, Schwierigkeitsgrad) wurden per Rückfrage geklärt: feste Mindestanzahl 2, Schwierigkeitsgrad bleibt der Redaktion überlassen. Bewertung läuft unverändert über den bestehenden `submitBlanks`/`checkBlanks`-Endpunkt (kein eigener F-115-Endpunkt nötig) — der zusätzliche Wortpool ist rein eine Formungs-/UI-Angelegenheit. Neue `BlanksSelectionStep`-Komponente (echtes Drag-and-Drop, wiederverwendet die dnd-kit-Bausteine aus F-114). Bewusst nur online. Siehe Architekturplanung Abschnitt 13.
- [x] **Mehrfachauswahl bei Multiple-Choice-Fragen (F-116) — aus dem Nutzer-Feedback vom 18.09.2026 (Version 0.25 des Anforderungskatalogs, erweitert F-21/Multiple Choice), vom Anforderungskatalog als "technisch wichtigster Einzelpunkt" des Feedback-Batches markiert.** **Erledigt 22.09.2026:** Neuer, eigenständiger Content-Typ `quiz_mc_multi` statt eines Flags auf `quiz_mc` — die geforderte Trennung ("bereits vorhandene Multiple-Choice-Inhalte bleiben unverändert einfachauswahl-basiert") ist damit strukturell erzwungen statt nur konventionell eingehalten. Die im Anforderungskatalog offen gelassene Bewertungsfrage (Alles-oder-nichts vs. anteilig) wurde per Rückfrage geklärt: Alles-oder-nichts, konsistent mit dem binären richtig/falsch-Modell, das Streaks/Fortschrittsquote/F-112-Feedback bereits nutzen (`checkMcMultiAnswer`, quiz-logic.ts). Neue `McMultiStep`-Komponente (Checkboxen statt Radio-Buttons, fester Hinweistext "Mehrere Antworten können richtig sein.", markiert nach Prüfung auch korrekte-aber-nicht-ausgewählte Optionen grün). Admin-Formular verlangt mindestens statt genau eine richtige Option. Bewusst nur online. Siehe Architekturplanung Abschnitt 13.
- [x] **Rollenmodell prüfen und ggf. vereinfachen (F-117) — aus dem Nutzer-Feedback vom 18.09.2026 (Version 0.25 des Anforderungskatalogs).** **Erledigt 22.09.2026:** Code-Audit vor der Rückfrage ergab, dass die dritte `user`-Rolle `content_editor` nie vergeben wurde und nirgends im Backend eine eigene Berechtigung freischaltete (alle Admin-Endpunkte verlangen ausschließlich `admin`) — ein reiner Karteileichen-Wert. Die im Anforderungskatalog offen gelassenen Fragen (Zielgruppe der Rückmeldung, welche Rollen tatsächlich unterschiedliche Berechtigungen brauchen) wurden per Rückfrage geklärt: die Rückmeldung bezieht sich vermutlich auf interne Test-/Redaktionsrollen (Eltern-/Unternehmens-Zugang sind für reguläre Lernende im normalen Betrieb ohnehin nicht sichtbar), `content_editor` wird entfernt, Eltern- (F-90) und Unternehmens-Zugang (F-91) bleiben unverändert, da beide nachweislich eigene, genutzte Berechtigungen haben. `user_role_check`-Constraint und `userRoleSchema` auf `learner`/`admin` reduziert (neue Migration `0023_black_zarda.sql`), da der Wert nie vergeben wurde ohne Auswirkung auf Bestandsdaten. Siehe Architekturplanung Abschnitt 13.
- [x] **Punktehamster (F-118) — aus dem Nutzer-Feedback vom 18.09.2026 (Version 0.25 des Anforderungskatalogs, erweitert F-67).** **Erledigt 22.09.2026:** Durchgängig sichtbares Maskottchen-Widget (`PunktehamsterWidget.tsx`, oberhalb der Tab-Leiste neben CompanyBranding/SponsorBanner statt im generischen `Header.tsx`) sammelt Fortschritt ("Futter", `user.mascot_food`) für jede richtig beantwortete Quiz-Frage — Karteikarten-Selbsteinschätzung zählt bewusst nicht mit. Die im Anforderungskatalog offen gelassenen Fragen (dauerhafte vs. session-gebundene Sichtbarkeit, Abschalt-Option) wurden per Rückfrage geklärt: durchgängig sichtbar, abschaltbar (`MascotSettings.tsx` im Einstellungen-Modal, Schalter `user.mascot_enabled`). Zähler wächst monoton (kein Reset, keine Belohnung als echte Währung — das ist F-119 vorbehalten), sinkt nie bei falschen Antworten. Neue `gamification.mascotStatus`-Query, Inkrement zentral in `recordQuizAttempt` (progress.ts, deckt auch offline synchronisierte Antworten ab). Siehe Architekturplanung Abschnitt 13.

**Content**
- [x] Erstes Thema/Lernfeld aus HB3 vollständig erstellen: Theorie-Zusammenfassung, Karteikarten, Übungsfragen (im Zwischenformat aus Iteration 0) — Thema 3.1 sowie gleich alle vier Themen von HB3 (3.1–3.4) im Content-Zwischenformat erstellt (`content/fachwirt-buero-projektorganisation/hb3/`) und per neuem Bulk-Import-Skript (`apps/api/src/db/import-content.ts`) in die Datenbank übernommen — löst den technischen Platzhalter-Content aus `db:seed` für diesen Kurs ab. Siehe Architekturplanung Abschnitt 13

**Testing**
- [x] Unit-Tests für die Spaced-Repetition-Logik (FSRS-Integration) — `apps/api/src/fsrs/scheduler.test.ts`, bereits mit der ursprünglichen FSRS-Implementierung entstanden, hier nur die Checkbox nachgezogen
- [x] End-to-End-Test: Registrierung → Karteikarten-Session → Quiz — `apps/api/test/core-learning-flow.integration.test.ts`, über die echte HTTP-Schicht (Fastify `app.inject()`, siehe `src/app.ts`, aus `index.ts` herausgezogen) gegen eine echte Testcontainers-Postgres-Instanz. Deckt ab: Registrierung (Erwachsene:r, direkte Session), Kursbeitritt, fällige Karteikarten laden + Selbsteinschätzung, Multiple-Choice-Quiz richtig beantworten (korrekte Option aus der DB gelesen statt im Content geraten) inkl. Fortschritt-Anstieg, Logout, sowie die Zugriffssperre ganz ohne Session-Cookie. Bewusst keine neue Browser-E2E-Infrastruktur (Playwright o. Ä.) — passt zum bereits festgelegten Vitest/Testcontainers-Testkonzept.

**Nutzer:innen-Feedback**
- [ ] Ersten kleinen Kreis Fachwirt-Kandidat:innen informell ansprechen und um Feedback zu diesem ersten Slice bitten

## Iteration 2 — Jugendschutz/Eltern-Consent scharf schalten

Ziel: Der vollständige Eltern-Consent-Flow und das Eltern-Dashboard stehen produktiv, **bevor** irgendein Kurs mit überwiegend minderjähriger Zielgruppe live geht. Diese Iteration wurde beim Entwickler-Review bewusst vor die Mathe-Kurs-Aktivierung (jetzt Iteration 3) gezogen.

**Programmierung (Kern)**
- [x] Vollständiger Eltern-Consent-Flow (F-08): E-Mail-Bestätigung, `CONSENT_TOKEN`, automatische Erinnerungen, kontoloser Vorschau-Modus — **Kernmechanismus umgesetzt und live verifiziert:** Registrierung unter 16-Jähriger fragt E-Mail eines Elternteils ab, legt `parent`/`parent_child_link`("pending")/`consent_token` an und versendet (aktuell nur simuliert, siehe `apps/api/src/email/sender.ts`) den Bestätigungslink; Login bleibt bis zur Bestätigung gesperrt (`auth.login`), die Bestätigungsseite (`/consent/confirm`) setzt `consent_status` auf "confirmed". **Kontoloser Vorschau-Modus umgesetzt und live verifiziert:** neuer öffentlicher `preview`-Router (`publicProcedure`, kein Login, kein Datenbank-Schreibzugriff) liefert 5 zufällige Demo-Fragen aus allen veröffentlichten Kursen, Frontend unter `/vorschau` (`apps/web/src/Vorschau.tsx`), verlinkt vom Sperrhinweis nach Registrierung einer/eines Minderjährigen. Prüf-/Formungslogik mit dem regulären Quiz-Modus über `apps/api/src/quiz-logic.ts` geteilt, UI-Schritt-Komponenten über `apps/web/src/QuizSteps.tsx`. **Automatische Erinnerungsmails umgesetzt und live verifiziert:** eigenständiges Wartungsskript `apps/api/src/db/send-consent-reminders.ts` (für periodischen externen Aufruf gedacht, z. B. Cron — kein eigener Scheduler/BullMQ, das ist erst ab Iteration 6 vorgesehen) verschickt bei unbestätigten `consent_token`-Zeilen alle 2 Tage (max. 3-mal) eine Erinnerung mit neu generiertem Bestätigungslink (rotiert `token_hash` auf derselben Zeile, da sich der ursprüngliche Klartext-Token nicht erneut verschicken lässt) und erhöht `reminder_sent_count`. Berücksichtigt korrekt einen zwischenzeitlichen Widerruf (`consent_status != "pending"` → keine weitere Erinnerung). Reine Entscheidungslogik in `apps/api/src/consent-reminder-logic.ts`, unit-getestet. Siehe Architekturplanung Abschnitt 13.
- [x] `PARENT`, `PARENT_CHILD_LINK` als eigener Account-Typ — Eltern haben jetzt einen echten Login: nach Bestätigung des Consent-Links wird automatisch eine Parent-Session angelegt (`consent.confirm`), das Eltern-Dashboard fordert dann einmalig das Setzen eines eigenen Passworts an (`parent.setInitialPassword`, löst den Platzhalter-Hash ab, siehe neue Spalte `parent.password_set`, Architekturplanung Abschnitt 13); danach regulärer Login über `parent.login`
- [x] Eltern-Dashboard-Grundgerüst (F-90): Einwilligungsstatus einsehen, Widerruf — `parent.me` (Liste verknüpfter Kinder mit Einwilligungsstatus) und `parent.revokeConsent` (setzt `consent_status = "revoked"`, sperrt den Kind-Login sofort über die bestehende Prüfung in `auth.login`) umgesetzt und live gegen echtes Postgres verifiziert, inkl. Frontend (`apps/web/src/ParentDashboard.tsx`, Route `/parent`). Bewusst als Sperre statt Hard-Delete umgesetzt (siehe Architekturplanung Abschnitt 13). **Noch offen:** Einblick in einzelne Kind-Berechtigungen wie F-66 (Gamification existiert noch nicht, siehe Iteration 6)
- [x] Kindgerechte Datenschutz-Kurzfassung (F-53) im Frontend einbinden — statische Seite `apps/web/src/DatenschutzKinder.tsx` unter `/datenschutz-kinder`, verlinkt aus dem Registrierungsformular (sobald `needsParentEmail`), vom Sperrhinweis nach der Registrierung und aus dem Eltern-Dashboard. **Wichtige Einschränkung:** F-53 ist laut Anforderungskatalog "ergänzend zur vollständigen juristischen Fassung (F-51)" gedacht — F-51 (Impressum/Datenschutzerklärung/AGB) existiert im Projekt noch gar nicht. Der Text ist deshalb bewusst als ungeprüfter Entwurf gekennzeichnet (Hinweis direkt auf der Seite), keine rechtlich verbindliche Datenschutzerklärung. Siehe Architekturplanung Abschnitt 13.

**Recht & Compliance**
- [ ] Externe Jugendschutzprüfung tatsächlich beauftragen — Zeitpunkt an diesen Content-/Entwicklungsfortschritt gekoppelt, nicht an ein Kalenderdatum (siehe Abschnitt 7)

**Content**
- [x] Restliche Themen/Lernfelder von HB3 erstellen (Priorität, siehe 80/20-Aufteilung, Abschnitt 9) — unkritisch bezüglich Jugendschutz, da der Fachwirt-Pilot weiterhin eine erwachsene Zielgruppe hat — alle vier Themen von HB3 (3.1–3.4) fertig, siehe Iteration 1. **Noch offen:** `fallaufgaben.md` (F-23) und `fachgespraech.md` (F-25) liegen als Content bereits im Zwischenformat vor, wurden aber bewusst noch nicht importiert, da die zugehörigen Features (Prüfungssimulation, Fachgesprächs-Trainer) im Code noch nicht existieren (siehe Phase 2/3)

**Testing**
- [x] End-to-End-Test des kompletten Eltern-Consent-Flows (Registrierung Minderjährige:r → Eltern-Mail → Bestätigung → Freischaltung → Widerruf) — `apps/api/test/consent-flow.integration.test.ts`, nach demselben `app.inject()`-Muster wie der Kernlernstrecke-Test (Iteration 1). Deckt ab: Registrierung einer minderjährigen Person bleibt ohne Session gesperrt, Elternteil bestätigt den Link (inkl. Auto-Login) und bekommt beim erneuten Öffnen desselben Links weiterhin eine Session ("already_confirmed", ohne die Einwilligung erneut zu zählen), das Kind kann sich danach einloggen, Elternteil setzt das erste Passwort und sieht das Kind im Dashboard, Elternteil kann sich mit dem neuen Passwort unabhängig neu einloggen, ein Widerruf sperrt das Kind sofort (FORBIDDEN mit "widerrufen"-Hinweis) und lehnt zusätzlich einen erneuten Klick auf den ursprünglichen Bestätigungslink ab (BAD_REQUEST, bewusst ein anderer Fehlerpfad als die Pending-Sperre). Siehe Architekturplanung Abschnitt 13.
- [x] Backend-Tests für `REPORT`/`BLOCK` (Datenmodell existiert bereits seit Iteration 0, auch ohne UI testbar) — sechs neue Testcontainers-Integrationstests in `apps/api/test/db.integration.test.ts`: Standard-`status` "offen", mehrfache Meldungen derselben Person ohne Unique-Constraint erlaubt, `report`/`block`-Kaskade beim Löschen des Kurses, Unique-Constraint gegen doppelte Blockierung, Kaskade beim Löschen der blockierten Person (`block.blocked_user_id`) — ergänzt die bereits bestehende Abdeckung der asymmetrischen `report`-Löschung im Rahmen von F-06

## Iteration 3 — Mehrfach-Kurs aktivieren, Mathe-Kurs live schalten

Ziel: Der zweite Kurs (Mathematik) existiert mit einem ersten Themenblock und wird jetzt — abgesichert durch den in Iteration 2 fertiggestellten Consent-Flow — für echte Nutzer:innen veröffentlicht; ein Nutzerkonto kann beide Kurse gleichzeitig belegen.

**Programmierung (Kern)**
- [x] Mehrfach-Kursbelegung (F-09) aktiv nutzen: Kursauswahl/-wechsel im Frontend, je Kurs getrennter Fortschritt — neue Komponente `apps/web/src/CourseSwitcher.tsx` (eingeschriebene Kurse als Auswahl-Tabs, weitere veröffentlichte Kurse zum Beitreten). `content.dueCards`/`content.theorySections`/`quiz.quizItems`/`progress.overview` nehmen jetzt eine `kursId` entgegen und filtern darauf, statt wie zuvor über alle eingeschriebenen Kurse hinweg zu aggregieren (gemeinsames Schema `activeKursInputSchema`, siehe Architekturplanung Abschnitt 13). Live gegen echtes Postgres mit zwei parallel belegten Kursen verifiziert: Kurswechsel zeigt sofort die richtigen Karteikarten/Quiz-Fragen/Theorie-Themen, `user_progress`-Einträge bleiben eindeutig dem jeweiligen Kurs zugeordnet.
- [x] Zweiten Kurstyp (Mathematik) im generischen Modell anlegen (`KURS.type = "schulfach"`, `KURS.metadata` mit Klassenstufe/Bundesland-Ansatz, siehe F-13), zunächst mit `is_published = false` — vollständiger Mathematik-9-Content (`content/mathematik-9/`, 3 Fachgebiete/7 Themen/217 Content-Items) über das bestehende, dafür generalisierte `apps/api/src/db/import-content.ts` importiert (`KURS_META`-Tabelle je Kurs-Slug, siehe Architekturplanung Abschnitt 13). Live gegen echtes Postgres verifiziert: Import korrekt (`type='schulfach'`, `metadata={klassenstufe:9,...}`), unveröffentlichter Kurs bleibt im Frontend komplett unsichtbar (kein Tab, kein Beitreten), nach temporärem `is_published=true` funktionieren Karteikarten/Theorie/Quiz/Fortschritt über den vollen Pfad, danach wieder auf `is_published=false` zurückgesetzt (Veröffentlichung erst nach Abschluss von Content/Admin-Bereich gemäß Zeile darunter). Dabei einen latenten Anzeige-Bug gefunden und behoben, der beim Fachwirt-Kurs (nur 1 Fachgebiet) nie auffiel: `fachgebiet.sort_order` wurde beim Import nie gesetzt (Spalten-Default 0 für alle Fachgebiete), wodurch Theorie- und Fortschritt-Ansicht bei mehreren Fachgebieten unterschiedliche, zufällige Reihenfolgen zeigten — siehe Architekturplanung Abschnitt 13.
- [x] Bulk-Import-Grundfunktion (F-17) bauen, die das Content-Zwischenformat einliest — löst das manuelle Iteration-0-Provisorium ab. **Umgesetzt 15.09.2026 als Admin-Trigger statt eines neuen Skripts:** Die bestehende `pnpm db:import-content`-Logik (Iteration 1) wurde aus `import-content.ts` als `importAllContent()` exportiert und über `admin.triggerImport` (`roleProcedure("admin")`) sowie einen Button in `apps/web/src/AdminPanel.tsx` aufrufbar gemacht — löst den bisherigen Weg ab, den Import nur per SSH/Terminal-Zugriff auf den Server auslösen zu können. Der reine CLI-Modus (`pnpm db:import-content`) bleibt unverändert erhalten (Guard über `import.meta.url`-Vergleich, damit der bloße Modul-Import durch `admin.ts` nicht versehentlich einen vollen Import auslöst und dabei den gemeinsamen DB-Pool des Servers schließt). Live verifiziert: CLI-Pfad weiterhin fehlerfrei, Admin-Trigger importiert erfolgreich (11 Dateien, 341 Content-Items), Server bleibt danach voll funktionsfähig, ein Learner-Konto erhält serverseitig 403 FORBIDDEN.
- [x] Admin-/Redaktionsbereich (F-11) in einer ersten, einfachen Version — bewusst zunächst nur Kurs-Veröffentlichung (nicht die volle CMS-artige Fragen-/Karteikarten-Pflege): neuer Router `admin` (`courses`/`setPublished`, `apps/api/src/trpc/routers/admin.ts`), gesichert über die bereits vorhandene, bis dahin ungenutzte `roleProcedure("admin")`. Frontend-Komponente `apps/web/src/AdminPanel.tsx`, nur für `role === "admin"` sichtbar (`App.tsx`). Löst den bisherigen Weg ab, `kurs.is_published` ausschließlich per direktem SQL-Zugriff zu setzen — direkt aus der Stakeholder-Review hervorgegangen (siehe Architekturplanung Abschnitt 13). Live gegen echtes Postgres verifiziert: Veröffentlichen/Zurückziehen wirkt sofort im Kurskatalog, ein Learner-Konto erhält serverseitig 403 FORBIDDEN bei einem direkten API-Aufruf. Die eigentliche Content-Pflege (CMS-Teil von F-11) folgte am 19.09.2026 (siehe oben, Iteration 1).
- [x] Nach Fertigstellung des ersten Mathe-Themenblocks: `KURS.is_published = true` setzen (erst jetzt zulässig, da der Consent-Flow aus Iteration 2 produktiv steht) — der Content für alle drei Themenblöcke liegt bereits vor (siehe unten), diese Aufgabe bleibt aber bewusst an die Programmierung/den Bulk-Import gekoppelt, nicht an den Content-Stand. **Erledigt 15.09.2026** über den bestehenden Admin-Button (`admin.setPublished`, siehe Iteration 3 oben) — bewusst über den echten Mechanismus statt eines direkten SQL-Updates, um ihn dabei gleich end-to-end zu verifizieren. Live gegen echtes Postgres geprüft: Kurs erscheint sofort im Katalog eines erwachsenen Lernkontos (Zielgruppe "alle"), Beitritt sowie Karteikarten/Theorie mit echtem Mathe-Content funktionieren; `fachgebiet.sort_order`-Fix aus Iteration 3 zeigt weiterhin die korrekte Themenreihenfolge (ALG1–3, GEO1–3, STO1). **Wichtiger Hinweis:** Die in Iteration 0 offen gebliebene Gegenprüfung des Mathe-Themenkatalogs mit echtem Schulbuchmaterial wurde bewusst NICHT vor dieser Veröffentlichung nachgeholt (explizite Nutzer-Entscheidung) — bleibt als offener Punkt in Iteration 0 bestehen und sollte zeitnah nachgezogen werden, siehe Architekturplanung Abschnitt 13.
- [x] **Quiz-Ergebnisse fließen in die Fortschrittsanzeige ein (F-26, nicht ursprünglich für diese Iteration vorgesehen, aber direkt aus der Stakeholder-Review hervorgegangen):** Bislang schrieb nur der Karteikarten-Modus in `user_progress` — eine Person, die viel Quiz übt, aber Karteikarten meidet, sah dauerhaft 0% Fortschritt. `quiz.submit*` (alle vier Formate) rufen jetzt `recordQuizAttempt` (`trpc/routers/progress.ts`) auf: richtige Antwort → `state = "review"` ("beherrscht"), falsche → `"learning"`; `difficulty`/`stability`/`dueAt` sind für Quiz-Zeilen neutrale Platzhalter, da `content.dueCards` ohnehin strikt auf `type = "karteikarte"` filtert. Bewusst NICHT im kontolosen Vorschau-Modus (`preview.ts`), der ohne jeden Datenbank-Schreibzugriff bleibt. `progress.overview` zählt jetzt Karteikarten UND alle vier Quiz-Typen (Theorie bleibt außen vor, kein Beherrschungs-Zustand). Live gegen echtes Postgres verifiziert: richtige Kurzantwort → sofort `state = "review"` und Fortschritt steigt; falsche Multiple-Choice-Antwort → `state = "learning"`, Fortschritt bleibt unverändert; Karteikarten-Fortschritt weiterhin unbeeinflusst. Siehe Architekturplanung Abschnitt 13.

**Content**
- [x] Erstes Mathe-Fachgebiet/Themenblock erstellen (z. B. Quadratische Funktionen, siehe Themenkatalog Abschnitt 4). **Erledigt 14.09.2026 — und zwar für alle drei Themenblöcke auf einmal** (Algebra & Funktionen, Geometrie, Stochastik), abweichend vom Validierungs-Gate (siehe Anforderungskatalog Abschnitt 9, 10 sowie Iteration-5-Hinweis unten). Siehe `content/mathematik-9/` im Repo.

**Testing**
- [x] Tests für Bulk-Import (Datenintegrität, Versionierung F-12) — neuer Testcontainers-Integrationstest `apps/api/test/import-content.integration.test.ts` gegen den echten Content aus `content/` (Repo-Root), nicht gegen synthetische Fixtures: prüft, dass je Content-Item genau eine `content_item_version` mit `version_number = 1` entsteht, dass ein erneuter Import den Content vollständig ersetzt statt verwaiste Versionen anzusammeln, dass `is_published` bei einem Re-Import nie überschrieben wird, und (Regressionstest) dass `fachgebiet.sort_order` weiterhin korrekt aus der Verzeichnisreihenfolge abgeleitet wird. Siehe Architekturplanung Abschnitt 13.

## Iteration 4 — Zweites Fachwirt-Handlungsgebiet (HB1) & Redaktions-Effizienz

Ziel: Der Fachwirt-Pilot hat zwei vollständige Handlungsbereiche, die Content-Erstellung ist durch bessere Werkzeuge spürbar schneller.

**Programmierung (Kern)**
- [x] Effizienzfunktionen im Redaktionssystem ausbauen (F-17): Vorlagen für Fragetypen, verbesserter Bulk-Export. **Erledigt 15.09.2026:** Zwei neue CLI-Werkzeuge in `apps/api/src/db/` — `content:scaffold` (Unterbefehle `new-thema`/`add-item`) erzeugt leere, korrekt formatierte Grundgerüste für neue Thema-Dateien bzw. einzelne Fragetyp-Blöcke (IDs automatisch aus vorhandenen Blöcken derselben Datei fortlaufend nummeriert), `db:export-content` schreibt den DB-Content zurück ins Zwischenformat (Gegenstück zu `db:import-content`, gitignored nach `content-export/`, siehe Architekturplanung Abschnitt 13 für die bewussten Einschränkungen). Reine Serialisierungs-Logik (`content-serializer.ts`) unit-getestet inkl. Round-Trip-Tests (serialisieren → mit dem bestehenden Parser wieder einlesen); live gegen die echte Datenbank verifiziert (Export aller 355 Content-Items, danach ein komplett neu erzeugtes Test-Thema mit allen fünf Fragetypen erfolgreich über den echten `db:import-content`-Pfad importiert, anschließend rückstandsfrei wieder entfernt).

**Content**
- [x] HB1 (Entscheidungsprozesse/Organisationsstrukturen) vollständig erstellen. **Erledigt 15.09.2026 — und zwar zusammen mit HB2 und HB4 auf einmal** (analog zur bereits zuvor beim Mathematik-Kurs getroffenen Entscheidung, siehe Iteration 3), abweichend vom eigentlich vorgesehenen KPI-gestaffelten Ausbau. Der Fachwirt-Pilot deckt damit bereits jetzt alle vier Handlungsbereiche der IHK-Prüfungsstruktur ab (17 Themen, 621 Content-Items nach Import). Themenlisten anhand des offiziellen DIHK-Rahmenplans verifiziert (siehe `content/README.md`). Neu eingeführt und ab HB1/HB2/HB4 verbindlich: ein `bloom`-Metadatenfeld (Bloom'sche Taxonomie, zusätzlich zur bisherigen `schwierigkeit`) — dafür `content_item.bloom` als neue, nullable Spalte ergänzt (Migration `0003_dusty_blockbuster.sql`), Parser/Import/die F-17-Werkzeuge aus der Zeile oben entsprechend erweitert, älterer Content (HB3, Mathematik-9, Demo) bleibt unverändert bei `bloom = null` statt eines irreführenden Defaults. Die ebenfalls neu eingeführte Frontmatter-Angabe `qualifikationsinhalte` bleibt bewusst rein dokumentarisch, ohne DB-Persistierung. Live gegen echtes Postgres verifiziert (voller Import, Stichproben der bloom-Werte, Round-Trip über `db:export-content`, App-Smoke-Test). Siehe Architekturplanung Abschnitt 13.

**Nutzer:innen-Feedback**
- [ ] Erste KPI-Auswertung (Abschnitt 11: aktive Nutzer:innen, Abschlussquote) für beide Kurse
- [ ] Validierungs-Gate für den Mathe-Kurs prüfen (Abschnitt 9): lohnt sich weiterer *Ausbau* (Programmierung/Redaktion) über den ersten live geschalteten Themenblock hinaus? **Hinweis (14.09.2026):** Der Content für alle drei Themenblöcke liegt bereits vor (siehe Iteration 3); dieses Gate entscheidet damit nicht mehr über weitere Content-*Erstellung*, sondern darüber, ob/wann die bereits vorhandenen weiteren Themenblöcke programmierseitig ausgerollt (Bulk-Import, `is_published`) und beworben werden.

## Iteration 5 — Ausbau der Kernlernerfahrung (entspricht Phase 2/3 im Anforderungskatalog)

Ziel: Das Kernlernangebot ist funktional vollständig für beide Kurse, bevor in aufwändigere Zusatzfunktionen (Iteration 6) investiert wird.

**Programmierung (Kern)**
- [x] Prüfungssimulation „Schriftliche Prüfung" (F-23). **Erledigt 16.09.2026:** Neuer Content-Typ `fallaufgabe` (Schema/Parser/Import) importiert `fallaufgaben.md` (Fachwirt) und `uebungsaufgaben.md` (Mathematik) — bislang bewusst übersprungen, da das Feature nicht existierte — insgesamt 21 mehrschrittige Situationsaufgaben. Neuer Router `exam` nutzt die dafür seit Abschnitt 4.3 vorbereiteten, bis dahin ungenutzten `exam_session`/`exam_answer`-Tabellen: `exam.start` wählt je Fachgebiet eine zufällige Fallaufgabe (Prüfung kombiniert mehrere Handlungsbereiche), `exam.submitAnswer` speichert die Selbsteinschätzung je Teilaufgabe (analog zum Karteikarten-Modus, da Fallaufgaben keine automatisch prüfbare Antwort haben), `exam.finish` berechnet den Gesamt-Score. Neuer Tab „Prüfung" (`apps/web/src/Exam.tsx`) mit wählbarer, rein clientseitiger Zeitbegrenzung (30 Min. bis 10 Std.), Musterlösungshinweise-Reveal und Punkte-Selbsteinschätzung je Teilaufgabe. Live gegen echte Postgres-Instanz verifiziert (vollständiger Durchlauf über 4 Fallaufgaben beim Fachwirt-Kurs, Score-Berechnung exakt nachgerechnet bestätigt; 3-Fachgebiete-Fall bei Mathematik-9 sowie der Leerfall „keine Fallaufgaben" beim Demo-Kurs geprüft). Siehe Architekturplanung Abschnitt 13.
- [x] Offline-Modus für individuelle Lernmodi (F-42). **Erledigt 16.09.2026 (alle sechs Bausteine):** `apps/api/src/fsrs/scheduler.ts` (reiner, DB-freier FSRS-Scheduler) nach `@edukedo/shared` verschoben, damit Client und Server künftig denselben Code für die Wiederholungsplanung nutzen. Neues `apps/web/src/offlineDb.ts` legt das lokale IndexedDB-Schema (über Dexie.js) an: `content` (offline verfügbar gemachte Karteikarten/Quiz-Items inkl. Lösung, siehe Architekturplanung Abschnitt 13) und `queue` (noch nicht synchronisierte Lern-Ereignisse). "Für offline verfügbar machen"-Button im "Fortschritt"-Tab (`apps/web/src/OfflineDownload.tsx`) lädt über den Endpunkt `offline.downloadKurs` (`apps/api/src/trpc/routers/offline.ts`) den vollständigen Karteikarten-/Quiz-Bestand des Kurses inklusive Lösung in `offlineDb.content`. Flashcards.tsx/Quiz.tsx erkennen per `useOnlineStatus`-Hook einen fehlenden Netzwerkzugriff und lesen dann aus `offlineDb.content` statt von `content.dueCards`/`quiz.quizItems` — Karteikarten-Bewertungen berechnen den nächsten FSRS-Zustand lokal (`apps/web/src/offlineFlashcards.ts`, `scheduleReview` aus `@edukedo/shared`), Quiz-Antworten werden lokal geprüft (`apps/web/src/offlineQuiz.ts`, nutzt dieselben `checkMcAnswer`/`checkMatching`/`checkBlanks`/`checkKurzantwort`/`shapeQuizItem`-Funktionen wie der Server — dafür `apps/api/src/quiz-logic.ts` ebenfalls nach `@edukedo/shared` verschoben). Beides schreibt ein Sync-Ereignis in `offlineDb.queue`, ohne dass ein Serverkontakt nötig ist. **Baustein 5, neu erledigt:** neuer Endpunkt `offline.syncQueue` spielt die gepufferten Ereignisse chronologisch über dieselben Pfade wie die Online-Mutationen nach — dafür `submitReview`s Kernlogik zu `applyReview` extrahiert und `recordQuizAttempt` um einen echten (offline erfassten) Zeitstempel erweitert, beide jetzt von `progress.ts` UND `offline.ts` genutzt (bewusstes Ereignis-Replay statt "Last Write Wins", siehe Architekturplanung Abschnitt 13). Neue Spalte `learning_event.client_event_id` (Migration `0007_narrow_sugar_man.sql`) dient als Idempotenz-Schlüssel, falls ein Sync-Versuch wiederholt wird. `App.tsx` löst den Sync automatisch bei jedem Wechsel zu `online` aus (`apps/web/src/offlineSync.ts`) und invalidiert bei Erfolg die Fortschritts-/Content-Abfragen. **Baustein 6, neu erledigt:** Sichtbare Online/Offline-Status-Anzeige im Header (`apps/web/src/OfflineStatus.tsx`) — zeigt "Offline" inkl. Anzahl noch nicht synchronisierter Ereignisse (`useOfflineQueueCount`, live über Dexie-Table-Hooks statt Polling), "Wird synchronisiert…" während eines laufenden Sync-Versuchs, eine 3 Sekunden sichtbare "✓ Synchronisiert"-Bestätigung danach, sowie einen bleibenden Fehlerhinweis, falls einzelne Ereignisse nicht übernommen wurden oder der Sync-Request selbst fehlschlug. Der bisher unsichtbare Sync-Trigger aus Baustein 5 wurde dafür von `App.tsx` in einen eigenen Hook (`useOfflineSync.ts`) verschoben, der seinen Zustand zurückgibt. Live verifiziert (echter Browser-Offline-Test über simulierte `online`/`offline`-Events: Karteikarten-Bewertung und eine Zuordnungs-Frage offline beantwortet, beim Wechsel zurück zu `online` automatisch synchronisiert — `learning_event`/`user_progress` in Postgres korrekt mit dem ursprünglichen Offline-Zeitstempel statt der Sync-Ankunftszeit befüllt, lokale Warteschlange danach leer; ein erneuter Sync-Versuch mit denselben Ereignis-IDs bestätigt Idempotenz — kein doppelter `learning_event`, `reps` unverändert; für Baustein 6 zusätzlich: Status-Pill zeigt "Offline · 1 noch zu synchronisieren" unmittelbar nach einer Offline-Antwort, "✓ Synchronisiert" nach erfolgreichem Sync mit automatischem Ausblenden nach 3 Sekunden, "Synchronisierung fehlgeschlagen" bei einem erzwungenen Fehlerfall (Fremdschlüssel-Verstoß durch ein manuell eingefügtes, ungültiges Warteschlangen-Ereignis), Rückkehr zum unauffälligen Normalzustand danach).
- [x] „Weiter lernen"-Einstieg (F-27). **Erledigt 16.09.2026:** Neuer Endpunkt `progress.suggestions` rankt Top-3-Themenvorschläge nach Fälligkeit (F-20) und Schwachstelle (F-32, direkt auf der `learning_event`-Infrastruktur von oben aufbauend), angezeigt als Vorschlagsleiste oberhalb der Tab-Navigation. Klick auf einen Vorschlag wechselt automatisch in den passenden Modus (Karteikarten bei Rückstand, sonst Quiz) UND filtert `content.dueCards`/`quiz.quizItems` gezielt auf das jeweilige Thema (neuer optionaler `themaId`-Parameter) statt nur kursweit — mit sichtbarem, aufhebbarem Filter-Badge. Live gegen echte Postgres-Instanz verifiziert (Vorschlag erscheint korrekt, Ein-Klick-Filterung funktioniert, Vorschlagsliste aktualisiert sich nach Beantworten, Kurswechsel setzt den Filter sauber zurück). Dabei einen vorbestehenden, unabhängigen Flaky-Test in `core-learning-flow.integration.test.ts` gefunden und behoben (verglich `mastered`-Zähler nur des ersten Fachgebiets, obwohl die beantwortete Zufallsfrage zu jedem Fachgebiet gehören kann). Siehe Architekturplanung Abschnitt 13.
- [x] Präsentationstrainer (F-24). **Erledigt 16.09.2026:** Neue Tabelle `presentation_draft` (genau ein Entwurf je Nutzer:in/Kurs — Gliederung relational als drei stabile Textspalten Einleitung/Hauptteil/Schluss, Checkliste als JSONB-Map, da die Punkte reine Frontend-Logik sind) und Router `presentation` (`get`/`save`, Upsert). Neuer Sub-Tab "Präsentation" unter "Prüfung" (`Pruefungsvorbereitung.tsx` bündelt F-23/F-24 als Segmented-Toggle, beide Kind-Komponenten bleiben wie beim Quiz-Tab immer gemountet, damit eine laufende Prüfungssitzung beim Wechsel nicht verloren geht) mit Gliederungs-Textfeldern, sieben Checklisten-Punkten und einer Redezeit-Stoppuhr (zählt bewusst über 10 Min. hinaus weiter statt hart abzubrechen — ein Übungswerkzeug ohne Zwang). Live gegen echte Postgres-Instanz verifiziert (Entwurf gespeichert, nach Seiten-Reload korrekt wiederhergestellt, Timer läuft, Sub-Tab-Wechsel verliert keinen Zustand). Siehe Architekturplanung Abschnitt 13.
- [x] Fachgesprächs-Trainer (F-25). **Erledigt 16.09.2026:** Neuer Content-Typ `fachgespraech_frage` importiert `fachgespraech.md` (nur beim Fachwirt-Piloten, 103 Fragen über alle vier Handlungsbereiche) — bislang bewusst ungenutzt, da das Feature nicht existierte. Reiner Fragen-Pool ohne Punkte/Musterlösung (anders als F-23-Fallaufgaben), daher genügt ein einzelner neuer Endpunkt `content.fachgespraechFragen` (zufällige Auswahl über den ganzen Kurs) ohne Sitzungs-/Scoring-Logik. Drittes Segment "Fachgespräch" im "Prüfung"-Tab (`Pruefungsvorbereitung.tsx`), zeigt Frage + Thema-Kontext, "Nächste Frage" schaltet weiter, am Ende "Neu mischen". Aufnahmefunktion zur Selbstreflexion (laut Anforderungskatalog nur "ggf.") bewusst zurückgestellt. Live gegen echte Postgres-Instanz verifiziert (Import über alle vier HB, vollständiger Durchlauf durch eine 20er-Runde, Neu-mischen-Funktion, Leerfall bei Mathematik-9 ohne Absturz). Dabei entdeckt: `exam_answer.content_item_version_id` verhindert per `ON DELETE RESTRICT` einen Re-Import von Themen mit bereits vorhandenen echten Prüfungsantworten — betraf hier nur eigene Test-Daten aus der F-23-Verifikation, für echte Lernendendaten aber relevant für künftige Content-Updates (siehe Architekturplanung Abschnitt 13).
- [x] Lernstatistiken, Schwachstellenanalyse (F-31/F-32). **Erledigt 16.09.2026:** Neue Tabellen `learning_event` (append-only Ereignis-Log je beantworteter Frage, Karteikarte wie Quiz) und `learning_session` (explizites Start/Heartbeat/Ende-Tracking, siehe `apps/web/src/useLearningSession.ts`) ergänzt, Migration `0004_wealthy_mastermind.sql`. Grund für die neuen Tabellen: `user_progress` speichert nur den aktuellen FSRS-Zustand, keine Historie — insbesondere Quiz-Antworten hinterließen dort bislang gar keine auswertbare Spur (siehe Architekturplanung Abschnitt 13). Neue `progress.stats`-Abfrage liefert Trefferquote/Anzahl/Zeitverlauf/Schwachstellen, dargestellt im bestehenden "Fortschritt"-Tab (`Progress.tsx`) statt eines eigenen Tabs. Live gegen echte Postgres-Instanz verifiziert (Registrierung, Karteikarten- und Quiz-Antworten, Tab-Wechsel und simulierter Sichtbarkeitswechsel lösen Start/Ende der Lernsitzung korrekt aus, Statistik- und Schwachstellen-Anzeige geprüft). Siehe Architekturplanung Abschnitt 13.
- [x] Restzeit-/Lernpensum-Anzeige inkl. beider Zielmodi (F-35). **Erledigt 16.09.2026:** Neuer Endpunkt `progress.pacing` plus `courses.setTarget`-Mutation, dargestellt im "Fortschritt"-Tab (`apps/web/src/Zielplanung.tsx`). "Lerneinheit" wird auf Thema-Ebene operationalisiert (siehe Architekturplanung Abschnitt 13). Zielmodus "einzeltermin" (z. B. Fachwirt-Pilot): Formel verbleibende Themen ÷ verbleibende Wochen bis `user_course.target_date`, mit Rückstands-Erkennung gegenüber dem ursprünglich ab `plan_start_date` geplanten Pensum (reine Formel in `apps/api/src/pacing.ts` ausgelagert, unit-getestet ohne DB, analog zu `fsrs/scheduler.ts`). Zielmodus "wochenziel" (z. B. für Kurse mit laufender statt punktueller Bewertung): neue Spalte `user_course.weekly_goal_items`, verglichen mit den `learning_event`-Zeilen der letzten rollierenden 7 Tage. Die im Anforderungskatalog vorgesehene "X von Y Handlungsbereichen verfügbar"-Anzeige bei unvollständigem Rahmenlehrplan wurde bewusst zurückgestellt, da beide Kurse bereits vollständig befüllt sind (siehe Iteration 4) — siehe Architekturplanung Abschnitt 13. Live gegen echte Postgres-Instanz verifiziert (Zieltermin setzen/löschen, Rückstands-Warnung, "Zieltermin bereits erreicht"-Zustand, Wochenziel setzen und nach einer Karteikarten-Antwort korrekt hochzählend, jeweils in beiden Zielmodi).
- [x] Barrierefreiheit gemäß WCAG 2.1 AA prüfen/nachziehen (F-44). **Erledigt 16.09.2026:** Punktueller Durchgang über die am häufigsten wiederverwendeten Bausteine statt einer Einzelseiten-Prüfung, da diese in praktisch jeder Ansicht auftauchen. Sprungmarke zum Hauptinhalt (`Header.tsx`, `.skip-link`) plus `id="main-content"` auf jedem `<main>`-Landmark (vorher hatte nur die eingeloggte App/Landing Page eines, alle anderen Seiten — Eltern-Dashboard, Vorschau, Datenschutz-Kinder, Consent-Bestätigung, Login/Registrierung — nur ein bloßes `<div>`). Die drei Header-/Segmented-Tab-Leisten (Lernmodus-Tabs, Login/Registrieren, Prüfungsvorbereitung) bekamen das vollständige ARIA-Tablist-Muster (`role="tablist"/"tab"/"tabpanel"`, `aria-selected`, roving Tabindex mit Pfeiltasten-Navigation über den neuen, gemeinsam genutzten `tabListKeyboardNav.ts`-Helfer) statt nur visuell markierter `<button>`-Gruppen. Header-Dropdowns (Kurs-/Nutzer-Menü) sind jetzt auch per Escape schließbar (vorher nur per Klick außerhalb) inkl. Fokus-Rückgabe an den Auslöser-Button (`useClickOutside.ts` zu `useDismissableMenu.ts` erweitert), plus `aria-haspopup`/`aria-expanded` am Auslöser. Die Flip-Karteikarte (`FlipCard.tsx`) hatte beide Seiten (Frage UND Antwort) unabhängig vom Umdreh-Zustand gleichzeitig für Screenreader zugänglich (`backface-visibility: hidden` wirkt nur visuell) — jetzt per `aria-hidden` je nach `flipped`-Zustand korrekt nur eine Seite, plus `aria-pressed` am Umdreh-Button. Elf verstreute `<p className="error">`-Fehlermeldungen zu einer gemeinsamen `ErrorMessage`-Komponente mit `role="alert"` zusammengeführt, damit Screenreader sie automatisch ankündigen. Dekorative Alert-Icons (`Icons.tsx`) bekamen `aria-hidden`. **Farbkontrast (Nutzer-Entscheidung 16.09.2026):** Weißer Text auf `--coral`/`--sprout`/`--danger` (Haupt-CTA-Button, aktive Tabs/Kacheln, Karteikarten-Rückseite, Löschen-Button) erreichte nur 2,8–4,4:1 statt der WCAG-AA-Mindestanforderung 4,5:1 — auf Nutzer-Wunsch behoben, indem drei neue, dediziert für Flächen-mit-weißem-Text kalibrierte Tokens (`--coral-fill`/`--sprout-fill`/`--danger-fill`, plus `--coral-fill-hover`) ergänzt wurden, statt die vielfach als Text-/Rahmenfarbe wiederverwendeten Basistöne selbst zu verändern (siehe Architekturplanung Abschnitt 13). Live gegen echte Postgres-Instanz verifiziert (Skip-Link per Tab-Fokus, Pfeiltasten-Navigation zwischen Tabs inkl. Fokuswechsel, Escape schließt Header-Dropdown mit Fokus-Rückgabe, Flip-Karte togglet `aria-hidden`/`aria-pressed` korrekt, neue Button-/Tab-Farben in Light- und Dark-Mode). Bewusst nicht Teil dieses Durchgangs: eine erschöpfende Einzelseiten-Prüfung jeder Ansicht (Admin-Panel, Präsentations-/Fachgesprächstrainer-Detailinteraktionen) — siehe Testing-Punkt unten.

**Content**
- [x] Weitere Handlungsbereiche (HB2, HB4) erstellen, je nach KPI-Signal aus Iteration 4. **Hinweis (14.09.2026):** Der „weitere Mathe-Themenblöcke"-Teil dieser Aufgabe entfällt — Geometrie und Stochastik liegen bereits seit Iteration 3 vollständig vor (siehe dort); hier verbleibt nur noch deren programmierseitiger Ausbau/Rollout (siehe Iteration 4) sowie eine Gegenprüfung mit Schulbuchmaterial (siehe Iteration 0), kein weiterer Content-Erstellungsaufwand. **Aktualisierung 15.09.2026:** HB2 und HB4 wurden inzwischen ebenfalls vollständig erstellt, zusammen mit HB1 in Iteration 4 (siehe dort) — auch dieser Teil entfällt damit als eigenständige Aufgabe.

**Testing**
- [x] Integrationstest für den F-42-Sync-Endpunkt (`offline.syncQueue`). **Erledigt 16.09.2026:** Neue `apps/api/test/offline-sync.integration.test.ts` (Testcontainers-Postgres, wie `core-learning-flow.integration.test.ts`) deckt genau die vier Randfälle ab, die der vollumfängliche Codereview über F-42 gefunden hatte — vorher gab es dafür nur manuelle Live-Verifikation: Idempotenz bei wiederholtem Sync-Versuch (kein doppeltes `reps`), ein älteres Offline-Ereignis nach einem bereits neueren Online-Review verändert `user_progress` nicht mehr (aber bleibt in `learning_event` protokolliert), ein Batch mit einem nicht existierenden UND einem deaktivierten Content-Item bricht nicht ab (nur diese beiden bleiben unsynchronisiert), derselbe client-generierte Eintrags-ID-Wert für zwei unterschiedliche Konten wird unabhängig verarbeitet. 5 neue Tests, `pnpm --filter @edukedo/api test` weiterhin vollständig grün (102 Tests). Siehe Architekturplanung Abschnitt 13.
- [ ] Manuelle Barrierefreiheits-Stichprobe (Screenreader)

## Iteration 6 — Validierungs-Gate bestanden: Social, Gamification, KI, Payment, Business-Lizenzen (entspricht Phase 4)

Ziel: Erst nach positivem Signal aus den KPIs (Abschnitt 11) werden die aufwändigeren, bislang zurückgestellten Funktionen gebaut.

**Programmierung (Kern)**
- [ ] Einladungs-/Freundschaftssystem (F-63), Highscore (F-60), Duelle (F-61), Lernpartner-Vermittlung (F-62). **Zwischenstand 17.09.2026 (F-63, Grundgerüst):** Neue Tabellen `invite_code` (je Kurs getrennt wie bei Mehrfach-Kursbelegung F-09 gefordert, `expires_at` bewusst `notNull()` — anders als beim Business-Lizenzcode F-91 verlangt der Anforderungskatalog hier ausdrücklich eine 7-Tage-Befristung, Code-Generierung `src/auth/invite-code.ts` wiederverwendet) und `friend_circle_link` (EINE symmetrische Zeile je Freundschaft, kanonisch sortiert nach User-ID per CHECK-Constraint, damit weder Duplikate noch Richtungsverwechslung entstehen können). Neuer `friend`-Router (`createInviteCode`/`inviteCodes`/`revokeInviteCode`/`redeemInviteCode`/`friends`, alle `protectedProcedure`). `redeemInviteCode` prüft Kurs-Einschreibung der einlösenden Person, lehnt Selbst-Einlösung ab, ist idempotent bei erneutem Einlösen, prüft gegenseitige Blockierungen (F-68, bereits bestehende `block`-Tabelle) mit derselben generischen "ungültig"-Meldung wie ein falscher Code (verrät der blockierenden Person nicht, dass ein Versuch stattfand), und begrenzt Einlöse-Versuche je Person auf 10 pro 10 Minuten (neues, bewusst einfaches In-Memory-Rate-Limiting in `src/auth/rate-limit.ts` — im Projekt gibt es noch keine Redis-Anbindung für allgemeines Rate-Limiting). Neue `FriendCircle.tsx`-Komponente im "Fortschritt"-Tab (neben Zielplanung/Offline-Download/Unternehmenscode-Einlösung). Highscore (F-60), Duelle (F-61), Lernpartner-Vermittlung (F-62) sowie die automatische Ergänzung um Kohorten-Mitgliedschaften (F-65, Kohorten existieren noch nicht) sind eigene, spätere Bausteine; F-66 (Minderjährigen-Einschränkung) nennt laut Anforderungskatalog ausdrücklich nur F-60–F-62, nicht F-63 selbst — ein reiner Freundeskreis-Eintrag ohne diese Funktionen hat für sich genommen keine Interaktions-/Fremdkontakt-Wirkung. Live gegen echte Postgres-Instanz verifiziert (Code erstellen/einlösen über zwei eingeschriebene Testkonten, beidseitig korrekte Freundesliste, erneutes Einlösen bleibt idempotent, Selbst-Einlösung abgelehnt, nicht eingeschriebene Person abgelehnt, abgelaufener Code abgelehnt, ein gegenseitig blockiertes Konto erhält dieselbe generische Fehlermeldung wie ein ungültiger Code, ab dem 10. Einlöse-Versuch innerhalb von 10 Minuten greift die Rate-Begrenzung, Widerrufen über die UI entfernt den Code sofort aus der Liste, Einlösen über die UI zeigt die Erfolgsmeldung und aktualisiert die Freundesliste ohne Reload). **Zwischenstand 17.09.2026 (F-60, Highscore-/Punkteliste):** Neue Spalte `user_course.highscore_opt_in` (Default `false`, "standardmäßig deaktiviert"). Neuer `highscore`-Router (`setOptIn`/`myOptIn`/`leaderboard`, alle `protectedProcedure`) — Punktestand = Anzahl richtig beantworteter Fragen der letzten 7 Tage (rollierendes Zeitfenster statt fester Kalenderwochen, kein Scheduler nötig), Liste beschränkt auf den eigenen Freundeskreis (F-63) je Kurs, zeigt nur opted-in Personen. `setOptIn` lehnt ein Opt-in durch eine minderjährige Person kategorisch ab (F-66: "höchstens mit gesonderter Einwilligung der Erziehungsberechtigten über das Eltern-Dashboard" — das granulare Berechtigungs-Werkzeug dafür existiert im Eltern-Dashboard noch nicht, siehe "Noch offen" in apps/web/README.md). Neue `Highscore.tsx`-Komponente im "Fortschritt"-Tab (nach `FriendCircle`) — zeigt für Minderjährige einen Hinweistext statt eines (potenziell irreführend deaktivierten) Kontrollkästchens. Duelle (F-61), Lernpartner-Vermittlung (F-62) und die Kohorten-Integration (F-65) bleiben eigene, spätere Bausteine. Live gegen echte Postgres-Instanz und über die UI verifiziert (Opt-in durch eine minderjährige Testperson korrekt mit `FORBIDDEN` abgelehnt, Bestenliste zeigt nur opted-in Freunde und schließt einen zwischenzeitlich nicht-opted-in Freund korrekt aus, gezielt eingefügte `learning_event`-Testdaten ergaben exakt die erwarteten Punktestände inkl. korrektem Ausschluss falscher Antworten und eines 10 Tage alten, außerhalb des 7-Tage-Fensters liegenden Treffers, Checkbox und Bestenliste funktionieren über die UI, Minderjährigen-Hinweistext erscheint korrekt statt der Checkbox). **Zwischenstand 17.09.2026 (F-62, Lernpartner-Vermittlung):** Neue, optionale Spalte `user_course.lernpartner_fachgebiet_id` (bevorzugter Handlungsbereich, `onDelete: "set null"`). Neuer `lernpartner`-Router (`setFachgebiet`/`matches`, beide `protectedProcedure`) — zeigt für jeden Freund im Kurs eine Übereinstimmung bei Zieltermin (±30 Tage, ein bewusst grober, im Anforderungskatalog nicht exakt vorgegebener Schwellenwert) und/oder identischem Handlungsbereich, absteigend nach Übereinstimmungsgrad sortiert. Bewusst ausschließlich anzeigend — kein Anfrage-/Bestätigungs-Workflow und kein Chat (Anforderungskatalog: "ohne offenes Forum/Chat"), der Kontakt läuft über die im Freundeskreis bereits sichtbare E-Mail-Adresse. Neue `Lernpartner.tsx`-Komponente im "Fortschritt"-Tab (nach `Highscore`), nutzt die bereits über `progress.overview` geladene Fachgebiets-Liste für das Präferenz-Auswahlfeld, statt eine zweite, redundante Abfrage einzuführen. Live gegen echte Postgres-Instanz und über die UI verifiziert (drei Testkonten mit unterschiedlichen Zielterminen/Handlungsbereich-Präferenzen ergaben exakt die erwartete Sortierung — beide Kriterien treffend zuerst, keines treffend zuletzt —, ein Handlungsbereich aus einem anderen Kurs wird bei der Präferenz-Auswahl korrekt abgelehnt).
- [x] **Melde-/Blockier-UI (F-68), zunächst innerhalb des Freundeskreises (F-63) — Datenmodell existiert bereits seit Iteration 0:** Neuer `report`-Router (`reportUser`/`blockUser`/`unblockUser`/`blockedUsers`, alle `protectedProcedure`) — verlangt bei beiden Aktionen eine bestehende Freundschaft in genau diesem Kurs als Voraussetzung, da der Freundeskreis aktuell die einzige Fläche ist, auf der eine Person überhaupt an die User-ID einer anderen kommt. Lehnt Selbst-Melden/-Blockieren ab. `blockUser` entfernt zusätzlich eine bestehende `friend_circle_link`-Zeile, damit eine blockierte Person nicht weiterhin in der eigenen Freundesliste steht. Neue Melden-/Blockieren-Buttons je Freund in `FriendCircle.tsx` (Melden blendet ein Begründungsfeld ein, Blockieren wirkt wie "Widerrufen"/"Lizenz entziehen" sofort per Klick) sowie eine neue "Meine Blockierungen"-Sektion mit Entblocken-Button. `admin`-Router um `reports` (offene Meldungen, Reporter-/Reported-E-Mail per Alias-Selbst-Join auf `user`) und `resolveReport` (setzt Status auf "geschlossen") erweitert, neue Sektion "Admin: Meldungen (F-68)" in `AdminPanel.tsx`. Highscore (F-60), Duelle (F-61), Lernpartner-Vermittlung (F-62) sind eigene, spätere Bausteine, mit denen laut Anforderungskatalog auch F-68 "zusammen gebaut" wird — deren Integration folgt dort. Live gegen echte Postgres-Instanz verifiziert (Melden zwischen zwei Freunden erfolgreich, Selbst-Melden abgelehnt, Melden ohne bestehende Freundschaft abgelehnt, Meldung erscheint korrekt in der Admin-Ansicht und verschwindet nach Schließen, Blockieren entfernt die Freundschaft beidseitig sofort und erscheint in der Blockierungsliste, ein neuer Einladungscode kann von der blockierten Person nicht eingelöst werden, Entblocken macht das erneute Einlösen wieder möglich, alles auch über die UI nachvollzogen).
- [x] **Nicht-soziale Gamification (F-67):** Neue Tabelle `achievement` (`userId`, `achievementKey`, `earnedAt`) — bewusst OHNE `kursId`, anders als `friend_circle_link`/`highscore`/`lernpartner`: ein Achievement würdigt die gesamte Lernreise über alle belegten Kurse hinweg (F-09), nicht eine einzelne Kurs-Mitgliedschaft. Achievement-Katalog (5 Einträge: Erster Schritt, Fleißig dabei, Auf Kurs, Eine Woche am Stück, Erste Prüfungssimulation) als Server-Konstante in `src/achievements/catalog.ts`, nicht DB-konfigurierbar. Neuer `gamification`-Router (`checkAndAward`/`myAchievements`/`myPersonalBests`, alle `protectedProcedure`) — `checkAndAward` berechnet bei jedem Aufruf neu, welche Kriterien erfüllt sind, und vergibt neu erfüllte Achievements idempotent (`onConflictDoNothing`); einmal vergebene Achievements bleiben unveränderlich, auch wenn die zugrunde liegenden Daten sich später ändern. `myPersonalBests` liefert vier On-the-fly-Kennzahlen (beste Tages-Trefferquote ab mind. 3 Antworten an diesem Tag, meiste Fragen an einem Tag, längste Lernserie in aufeinanderfolgenden Tagen, beste Prüfungspunktzahl). Neue `Achievements.tsx`-Komponente im "Fortschritt"-Tab (nach `Lernpartner`) — ruft `checkAndAward` beim Mounten auf und zeigt bei neu erreichten Achievements eine Erfolgsmeldung. Live gegen echte Postgres-Instanz und über die UI verifiziert (gezielt eingefügte `learning_event`/`exam_session`-Testdaten über eine 7-Tage-Serie ergaben exakt die erwarteten vier neu freigeschalteten Achievements — "Auf Kurs" korrekt NICHT freigeschaltet, da nur 10 statt 100 richtige Antworten — sowie exakt die erwarteten Bestwerte 83 % Trefferquote/6 Fragen/7 Tage Serie/82 % Prüfung; ein erneuter Aufruf lieferte keine neuen Achievements mehr).
- [ ] Kohorten-/Dozenten-Funktion (F-07, F-64, F-65)
- [ ] KI-gestützte Bewertung & Aufgabengenerierung (F-70–F-72), inkl. BullMQ/Redis-Warteschlange
- [x] **Business-Lizenzen & Sponsoring (F-91–F-94, ergänzt 14.09.2026, siehe Architekturplanung Abschnitt 4.5/13):** `company_account` als eigener Account-Typ/Login (Rolle `company_admin`, analog zu `parent`) inkl. Lizenzkontingent-Verwaltung (`seat_limit`, `billing_status`); `company_invite_code` zur Lizenzvergabe (analog F-63); `user_company_membership` zur Branding-/Statistik-Zuordnung (1:1 je Nutzer:in). **Zwischenstand 16.09.2026 (Baustein 1 von 6, Datenmodell + Auth-Grundgerüst):** `company_account`-Tabelle (inkl. `seat_limit`/`billing_status`/`branding_*`, Letztere bis Baustein 3 ungenutzt) sowie `company_setup_token` neu; `session` um `company_account_id` erweitert (analog zu `parent_id`, `num_nonnulls`-Check jetzt über drei Spalten). Ein Admin legt das Konto über `admin.createCompanyAccount` an (bewusst kein Self-Service-Signup, Abrechnung läuft manuell außerhalb des Systems) — das Unternehmen erhält wie ein neu angelegter Elternteil einen nirgends bekannten Platzhalter-Passwort-Hash, ein per (simulierter) E-Mail verschickter Einmal-Setup-Link (`company.confirmSetup`, analog zu `consent.confirm`) verschafft die erste Session, aus der heraus `company.setInitialPassword` ein echtes Passwort setzt. Neuer `company`-Router (`confirmSetup`/`login`/`logout`/`setInitialPassword`/`me`), neue `requireCompanyAdmin`-Middleware/`protectedCompanyAdminProcedure` (analog zu `requireParent`), neue Routen `/company` (`CompanyDashboard.tsx`, analog zu `ParentDashboard.tsx`) und `/company/setup` (`CompanySetup.tsx`, analog zu `ConsentConfirm.tsx`). Admin-Bereich zeigt bestehende Unternehmens-Konten und ein Anlage-Formular (inkl. `devSetupUrl`, solange kein echter E-Mail-Versand angebunden ist, analog zu `devConfirmUrl`). **Baustein 2, neu erledigt (Einladungscodes + Mitgliedschaft):** Neue Tabellen `company_invite_code` (kurzer, per Hand eintippbarer Code — `src/auth/invite-code.ts` — bewusst OHNE Pflicht-Befristung anders als beim späteren F-63, das Sitzplatz-Kontingent ist die eigentliche Grenze) und `user_company_membership` (1:1 je Nutzer:in). `company`-Router erweitert um `createInviteCode`/`inviteCodes`/`revokeInviteCode` sowie `members`/`revokeMembership` (bewusst nur E-Mail + Beitrittsdatum, kein Lernfortschritt — Beschäftigtendatenschutz) für `company_admin`, plus `redeemInviteCode` (`protectedProcedure`, von der Lernperson selbst aufgerufen) mit hart durchgesetztem `seat_limit`, Ablehnung bei Zuordnung zu einem anderen Unternehmen und idempotentem Erfolg bei erneutem Einlösen desselben Codes/Unternehmens. Neue `RedeemCompanyCode.tsx`-Komponente im "Fortschritt"-Tab (neben Zielplanung/Offline-Download). **Baustein 3, neu erledigt (Branding, F-92):** `company.updateBranding` (`protectedCompanyAdminProcedure`) pflegt die seit Baustein 1 vorhandenen, bis dahin ungenutzten `branding_*`-Spalten (leerer String löscht ein Feld auf `null`, kein Datei-Upload — die Plattform hat noch keine Objektspeicher-Anbindung, ein Unternehmen verlinkt ein extern gehostetes Logo); neue `BrandingSection` in `CompanyDashboard.tsx` mit Live-Vorschau in derselben Aufmachung wie das spätere Lernenden-Banner. Neuer `company.myBranding`-Query (`protectedProcedure`, `null` ohne Mitgliedschaft) liefert das Branding des zugeordneten Unternehmens; neue `CompanyBranding.tsx`-Komponente zeigt es als Banner oberhalb der Lernmodus-Tabs in der App der Lernenden (bewusst ohne Interaktivität/Tracking — reiner Reputationswert, keine Werbefläche, Abgrenzung zum späteren Sponsoring F-94). `RedeemCompanyCode.tsx` invalidiert nach erfolgreichem Einlösen zusätzlich `company.myBranding`, damit das Banner ohne Seiten-Neuladen erscheint (Codereview-Fund während der Live-Verifikation: ohne diese Invalidierung blieb der zuvor gecachte `null`-Stand bis zum nächsten vollständigen Laden bestehen). **Baustein 4, neu erledigt (aggregierte Statistik, F-93):** Neuer `company.stats`-Query (`protectedCompanyAdminProcedure`) liefert drei Kennzahlen — Anteil aktiver Lizenzen (mind. ein `learning_event` in den letzten 30 Tagen), Ø Trefferquote und Ø Fortschritt (Anteil `user_progress`-Zeilen im Zustand `review`) — als reine SQL-Aggregation (`count`/`count distinct` über Joins auf `user_company_membership`, parallel abgefragt), sodass der Endpunkt strukturell nie Einzel-Nutzer-Zeilen zurückgeben kann. Neue Mindestgröße `MIN_COHORT_SIZE_FOR_STATS = 5`: Unterhalb dieser Mitgliederzahl liefert der Endpunkt bewusst `null` statt einer Kennzahl, da eine "aggregierte" aber aus z. B. nur einer Person berechnete Kennzahl faktisch eine personenbezogene Einzelauswertung wäre. Neue `StatsSection` in `CompanyDashboard.tsx` im selben `stat-section`/`stat-tile`-Vokabular wie "Deine Lernstatistik" in `Progress.tsx`, mit erklärendem Hinweis statt leerer Werte unterhalb der Mindestgröße. **Baustein 5, neu erledigt (Sponsoring, F-94):** Neue `sponsor`-Tabelle (`name`, `logo_url`, `attribution_text`, `kurs_id` nullable = plattformweit, `is_active`, `starts_at`/`ends_at`, jeweils unabhängig voneinander optional) — bewusst vollständig vom Lizenzmodell (F-91) getrennt, keine Nutzer-Verknüpfung. Neuer öffentlicher `sponsor.list`-Query (`publicProcedure`, kein Login nötig) liefert plattformweite Sponsorings plus, bei gesetzter `kursId`, zusätzlich die für genau diesen Kurs hinterlegten — gefiltert auf `is_active` und ein etwaiges Zeitfenster. `admin`-Router um `sponsors`/`createSponsor`/`setSponsorActive` (`roleProcedure("admin")`) erweitert, neue Sektion "Admin: Sponsoring (F-94)" in `AdminPanel.tsx` (Anlage-Formular inkl. Kurs-Auswahl "Plattformweit" vs. einzelner Kurs, Aktivieren/Deaktivieren analog zum Kurs-Veröffentlichen-Muster statt Löschen). Neue `SponsorBanner.tsx`-Komponente zeigt alle zutreffenden Sponsorings oberhalb der Lernmodus-Tabs — bewusst ohne Klick-Handler/Personalisierung (N-01/N-13), sichtbar für ALLE Lernenden (nicht nur Unternehmens-Mitglieder wie das Branding-Banner aus Baustein 3). Damit ist F-91–F-94 bis auf das optionale Admin-Freischalt-Werkzeug für die Abrechnung (Baustein 6) vollständig umgesetzt. Live gegen echte Postgres-Instanz verifiziert (Konto anlegen, Setup-Link öffnen, Passwort setzen, Logout/Login, falsches Passwort abgelehnt, wiederholter Setup-Link liefert `already_confirmed`, eine Company-Session kann keine `admin.*`-Endpunkte aufrufen, doppelte Kontakt-E-Mail beim Anlegen abgelehnt; für Baustein 2 zusätzlich: Code einlösen inkl. Groß-/Kleinschreibungs-Normalisierung, Sitzplatz-Zähler steigt/sinkt korrekt, drittes Einlösen bei `seat_limit = 2` korrekt abgelehnt, ein zweites Unternehmen kann einer bereits zugeordneten Person keine Lizenz zuweisen, erneutes Einlösen desselben Codes durch dieselbe Person bleibt idempotent erfolgreich, Mitgliederliste zeigt ausschließlich E-Mail/Beitrittsdatum, Lizenzentzug senkt den Sitzplatz-Zähler sofort; für Baustein 3 zusätzlich: Branding speichern und im Dashboard nach Reload weiterhin angezeigt, Banner erscheint ohne Reload direkt nach Code-Einlösung, Banner bleibt über alle Lernmodus-Tabs und einen vollständigen Seiten-Reload hinweg sichtbar, Banner verschwindet nach Widerruf der Mitgliedschaft, kein Banner ohne Mitgliedschaft; für Baustein 4 zusätzlich: bei 3 Mitgliedschaften liefert `company.stats` erwartungsgemäß `null`-Kennzahlen samt Hinweistext, ab 5 Mitgliedschaften mit gezielt eingefügten `learning_event`/`user_progress`-Testdaten stimmten alle drei berechneten Prozentwerte exakt mit der händisch erwarteten Rechnung überein; für Baustein 5 zusätzlich: plattformweiter Sponsor erscheint auch ohne gewählten Kurs, kursspezifischer Sponsor erscheint zusätzlich nur im richtigen Kurs, ein anderer Kurs zeigt ihn korrekt nicht, ein zukünftiger `starts_at` und ein vergangener `ends_at` blenden das jeweilige Sponsoring korrekt aus, Deaktivieren im Admin-Bereich entfernt das Banner sofort nach Reload). **Baustein 6, neu erledigt (Admin-Freischalt-Werkzeug für die Abrechnung):** Neue `admin.updateCompanyBilling`-Mutation (`roleProcedure("admin")`) pflegt `billing_status`/`seat_limit` gemeinsam in einem Endpunkt (beide Felder werden in der Praxis nach demselben manuellen Zahlungseingang aktualisiert, nie unabhängig voneinander) — kein automatisierter Checkout, keine Anbindung an den separaten Payment-Service. Neue `CompanyBillingForm` je Zeile in der Unternehmens-Konten-Liste des `AdminPanel.tsx` (Dropdown Ausstehend/Aktiv/Abgelaufen + Sitzplatz-Kontingent-Eingabe + Speichern-Button). Damit ist F-91–F-94 vollständig umgesetzt. Live gegen echte Postgres-Instanz verifiziert (Konto anlegen, Setup-Link öffnen, Passwort setzen, Logout/Login, falsches Passwort abgelehnt, wiederholter Setup-Link liefert `already_confirmed`, eine Company-Session kann keine `admin.*`-Endpunkte aufrufen, doppelte Kontakt-E-Mail beim Anlegen abgelehnt; für Baustein 2 zusätzlich: Code einlösen inkl. Groß-/Kleinschreibungs-Normalisierung, Sitzplatz-Zähler steigt/sinkt korrekt, drittes Einlösen bei `seat_limit = 2` korrekt abgelehnt, ein zweites Unternehmen kann einer bereits zugeordneten Person keine Lizenz zuweisen, erneutes Einlösen desselben Codes durch dieselbe Person bleibt idempotent erfolgreich, Mitgliederliste zeigt ausschließlich E-Mail/Beitrittsdatum, Lizenzentzug senkt den Sitzplatz-Zähler sofort; für Baustein 3 zusätzlich: Branding speichern und im Dashboard nach Reload weiterhin angezeigt, Banner erscheint ohne Reload direkt nach Code-Einlösung, Banner bleibt über alle Lernmodus-Tabs und einen vollständigen Seiten-Reload hinweg sichtbar, Banner verschwindet nach Widerruf der Mitgliedschaft, kein Banner ohne Mitgliedschaft; für Baustein 4 zusätzlich: bei 3 Mitgliedschaften liefert `company.stats` erwartungsgemäß `null`-Kennzahlen samt Hinweistext, ab 5 Mitgliedschaften mit gezielt eingefügten `learning_event`/`user_progress`-Testdaten stimmten alle drei berechneten Prozentwerte exakt mit der händisch erwarteten Rechnung überein; für Baustein 5 zusätzlich: plattformweiter Sponsor erscheint auch ohne gewählten Kurs, kursspezifischer Sponsor erscheint zusätzlich nur im richtigen Kurs, ein anderer Kurs zeigt ihn korrekt nicht, ein zukünftiger `starts_at` und ein vergangener `ends_at` blenden das jeweilige Sponsoring korrekt aus, Deaktivieren im Admin-Bereich entfernt das Banner sofort nach Reload; für Baustein 6 zusätzlich: Statusänderung auf "Aktiv" und Kontingent-Erhöhung auf 25 landen sofort in der Admin-Liste und im `company.me`-Abruf der betroffenen Unternehmens-Session selbst, eine unbekannte Konto-ID liefert `NOT_FOUND`, eine Company-Session kann den Admin-Endpunkt selbst nicht aufrufen (`UNAUTHORIZED`)).
- [x] Visuelles Unternehmens-Branding (F-92): Logo/Farbschema/Begrüßungstext auf Basis von `company_account`, rein präsentationsseitig — kein separater Content
- [x] Aggregierte Unternehmens-Statistik (F-93) als eigener `/company/*`-Endpunkt, technisch ohne Möglichkeit einer Einzel-Nutzer-Auswertung (Beschäftigtendatenschutz, § 26 BDSG — siehe Anforderungskatalog Abschnitt 7, Architekturplanung Abschnitt 8)
- [x] Sponsoring-Markenplatzierung (F-94): `sponsor`-Tabelle, statische Anzeige-Komponente ohne Tracking/Personalisierung, admin-gepflegt; für den Schulfach-Kurs zusätzlich gegen N-01/N-13 prüfen (keine Interaktivität/Call-to-Action)
- [x] Einfaches Admin-Werkzeug, um `company_account.billing_status`/`seat_limit` nach manuellem Zahlungseingang freizuschalten (kein automatisierter Checkout, siehe Architekturplanung Abschnitt 13)

**Programmierung (Payment)**
- [ ] Payment-Service als eigenständiges App-Paket aufsetzen (eigene DB, eigenes Deployment)
- [ ] Event-/Message-Queue Kern ↔ Payment (BullMQ auf Redis) — inkl. Anbindung der bereits bestehenden Konto-Selbstlöschung (F-06) aus Iteration 1, damit gelöschte Konten auch im Payment-Service bereinigt werden
- [ ] Abo-/Kaufverwaltung (F-81), Statusübersicht im Nutzerprofil (F-82)

**Recht & Compliance**
- [ ] Vertragspartner-AGB gegenüber Minderjährigen prüfen (Zahlungsdienstleister, Managed-API-Anbieter)
- [ ] Zahlungsdienstleister auswählen und Vertrag abschließen (setzt die in Iteration 0 angemeldete Rechtsform voraus)
- [ ] **Einfache Unternehmens-AGB/Nutzungsbedingungen für Business-Lizenzen erstellen (ergänzt 14.09.2026):** Regelt Rechnungsstellung, Laufzeit/Kündigung des Kontingents sowie den Hinweis, dass der zugrunde liegende Lerncontent unabhängig davon weiterhin frei zugänglich bleibt (siehe Anforderungskatalog Abschnitt 5.12) — unabhängig von der B2C-AGB-Prüfung oben

**Testing**
- [ ] Kontrakttests Kern ↔ Payment
- [ ] Event-/Queue-Tests (Idempotenz, Verhalten bei Ausfall)
- [ ] **Zugriffskontroll-Tests für `/company/*`-Statistik-Endpunkte (ergänzt 14.09.2026):** Verifizieren, dass mit `company_admin`-Berechtigung unter keinen Umständen Einzel-Nutzer-Datensätze abrufbar sind — nur aggregierte Werte
- [ ] **Test der Lizenzkontingent-Grenzen (ergänzt 14.09.2026):** Einladungscode lässt sich nicht über `seat_limit` hinaus einlösen; Branding erscheint nur für Mitglieder des jeweiligen `company_account`

## Iteration 7 — Navigation & Informationsarchitektur-Überarbeitung (F-100–F-107)

Ziel: Die Navigation im eingeloggten Bereich trägt eine perspektivisch deutlich größere Kurs-/Content-Menge, statt nur für die aktuellen zwei Pilotkurse zu funktionieren; Lernmodus- und Fortschritt-Bereich sind aufgeräumter strukturiert. Anders als die bisherigen Iterationen kein additiver Funktionsbereich, sondern ein Umbau bereits produktiver UI — betrifft primär `apps/web/src/App.tsx`, `Header.tsx`, `CourseSwitcher.tsx`, `Theorie.tsx`, `Quiz.tsx`/`Flashcards.tsx`, `Progress.tsx`, `UserMenu.tsx` sowie `apps/api/src/trpc/routers/courses.ts`.

**Programmierung (Kern)**
- [x] Dedizierte Kursauswahl-/Katalogseite (F-100) mit Suche/Filter/Kategorien; Header-Dropdown (`CourseSwitcher.tsx`) zeigt danach nur noch die aktuell belegten Kurse plus einen Link zu dieser Seite, nicht mehr den vollständigen Katalog. **Erledigt 18.09.2026:** neue `CourseSelection.tsx` (Suche per Titel-Teilstring + drei Kategorie-Filter-Buttons), als eigener `view`-Zustand `"courses"` (analog zu `"admin"`). Siehe Architekturplanung Abschnitt 13.
- [x] Verbindliche Lernbereichsauswahl vor Content-Zugriff (F-101): sowohl direkt nach der Registrierung als auch bei jeder weiteren Kursanmeldung über die neue Auswahlseite (F-100) — kein Zugriff auf Lerninhalte ohne mindestens eine getroffene Auswahl. **Erledigt 18.09.2026:** abgeleiteter `showCourseSelection`-Zustand in `App.tsx` erzwingt `CourseSelection.tsx` (ohne "← Zurück"), solange `courses.list` geladen ist und keine Belegung existiert. Live verifiziert: brandneues Konto landet zwangsweise auf der Kursauswahl. Siehe Architekturplanung Abschnitt 13.
- [x] Kurskategorie als neues Feld im generischen `kurs.metadata`-JSON (z. B. `kategorie: "erwachsenenbildung" | "schule"`, siehe Anforderungskatalog Abschnitt 4/13) einführen und für die bestehenden Kurse befüllen (Fachwirt-Pilot: `erwachsenenbildung`; Mathematik-9 und Demo-Kurs: `schule` bzw. unkategorisiert). **Erledigt 18.09.2026** in `import-content.ts` (`KURS_META`) sowie `course-audience.ts` (`kursKategorie`-Helfer, analog zu `kursZielgruppe`).
- [x] Belegungs-Exklusivität für Erwachsenenbildungs-/Weiterbildungskurse (F-102, schränkt F-09 ein): `courses.enroll` lehnt eine zweite gleichzeitige aktive Belegung eines Kurses dieser Kategorie ab (bzw. bietet einen expliziten Wechsel-Flow „bestehende Belegung verlassen und neue beginnen" an). **Erledigt 18.09.2026:** integrierter Wechsel-Flow mit Bestätigung (Nutzer-Entscheidung) statt zwei getrennter Schritte — `CourseSelection.tsx` erkennt den Konflikt clientseitig und ruft `courses.enroll` mit `leaveKursId` auf; `courses.enroll` prüft zusätzlich serverseitig (409 bei fehlendem/falschem `leaveKursId`) und führt den Wechsel atomar in einer DB-Transaktion aus. Neue, allgemeine `courses.leave`-Mutation. Schulkurse bleiben von der Einschränkung unberührt. Siehe Architekturplanung Abschnitt 13.
- [x] Tab „Theorie" aus der Haupt-Navigation entfernen (F-103); `content.theorySections`-Endpunkt und zugehöriger Content bleiben unverändert im Datenmodell erhalten. **Erledigt 18.09.2026:** Theorie-Inhalte bleiben vollständig ohne Zugriffsweg im eingeloggten Bereich — kein Ersatz-Tab, keine Einbindung im neuen „Lernen"-Tab (F-104); `Theorie.tsx` als jetzt unbenutzte Komponente gelöscht. Siehe Architekturplanung Abschnitt 13.
- [x] Vereinheitlichter Lernmodus-Tab **„Lernen"** (F-104, Name entschieden am 18.09.2026). **Erledigt 18.09.2026:** `Quiz.tsx`/`Flashcards.tsx` bleiben unverändert bestehen, neue `Lernen.tsx` verteilt je Präferenz an eines der beiden oder an die neue `MixedLearning.tsx` (echte gemischte Warteschlange aus fälligen Karteikarten + Quiz-Runde, inkl. Offline-Unterstützung). Präferenz als zwei neue `user`-Spalten (`learn_flashcards_enabled`/`learn_quiz_enabled` + `learning_mode_preference_set`, Migration `0016_magical_miss_america.sql`) statt einer dritten "Beides"-Option — DB-Check-Constraint erzwingt "mindestens eine aktiv". Neue Erstbesuch-Abfrage (`LearningModePrompt.tsx`) sowie Einstellungen-Checkboxen (`LearningModeSettings.tsx`, vorerst im bestehenden Fortschritt-Tab). Live verifiziert (Desktop/Mobile/Dark Mode): 40er-Mischliste (20 Karteikarten + 20 Quiz) mit korrektem Interleaving, Checkbox-Sperre bei "mindestens eine aktiv" funktioniert. Neuer Integrationstest in `core-learning-flow.integration.test.ts`. Siehe Architekturplanung Abschnitt 13.
- [x] Neuer, zunächst leerer Platzhalter-Tab „Instrumente" (F-105) in der Haupt-Navigation. **Erledigt 18.09.2026** zusammen mit F-103/F-104.
- [x] Tab „Prüfung" unverändert übernehmen (F-106) — keine funktionale Änderung. **Erledigt/verifiziert 18.09.2026** zusammen mit F-103/F-104 (unverändert in der neu sortierten Tab-Leiste enthalten).
- [x] Restrukturierung des Tabs „Fortschritt" (F-107): Unter-Tab „Einstellungen" wandert ins Header-Benutzermenü (`UserMenu.tsx`, als Modal); Unter-Tabs „Sozial" und „Erfolge" werden zu eigenständigen Haupt-Tabs auf einer Ebene mit den übrigen; Unter-Tab „Übersicht" entfällt, sein Inhalt erscheint direkt beim Anklicken von „Fortschritt". **Erledigt 18.09.2026:** neue `Sozial.tsx` (FriendCircle/Highscore/Lernpartner) und `SettingsModal.tsx` (analog zu `DeleteAccount.tsx`); `Achievements.tsx` unverändert als eigener Tab "Erfolge"; `Progress.tsx` zeigt nur noch die bisherige Übersicht. Dabei zwei Bugs gefunden und behoben (Modal entmountete sich selbst beim Öffnen; `.tab-nav`-Grid war fest auf 5 Spalten codiert). Siehe Architekturplanung Abschnitt 13. Damit ist Iteration 7 (F-100–F-107) vollständig abgeschlossen.

**Content**
- [ ] Rückwirkende Ergänzung des `bloom`-Metadatenfelds bei HB3 (Fachwirt-Pilot) und allen drei Themenblöcken des Mathematik-Kurses (siehe Anforderungskatalog Abschnitt 4/10, offener Punkt 3) — bislang nur bei HB1/HB2/HB4 vorhanden

**Testing**
- [x] End-Test der neuen Kursauswahl-Pflicht (F-101): **Erledigt 18.09.2026** — live über die UI verifiziert (brandneues Konto landet zwangsweise auf der Kursauswahl, kein "← Zurück", nach Beitritt direkt in der App); kein zusätzlicher Integrationstest, da rein clientseitige Anzeigelogik ohne neue Backend-Regel.
- [x] Test der Belegungs-Exklusivität (F-102): **Erledigt 18.09.2026** — neuer `course-enrollment.integration.test.ts` (Ablehnung ohne `leaveKursId` mit 409, atomarer Wechsel mit `leaveKursId`, unveränderte Mehrfachbelegung bei Schulkursen, `courses.leave` inkl. 404 bei nicht belegtem Kurs) sowie `course-audience.test.ts` (`kursKategorie`/`isEnrollmentExclusive`).
- [x] Test des „Lernen"-Tabs (F-104): **Erledigt 18.09.2026** — Integrationstest deckt die Ablehnung der ungültigen Kombination "beide aus" (400) sowie eine erfolgreiche Präferenzänderung ab (`core-learning-flow.integration.test.ts`); die drei gültigen Kombinationen (nur Karteikarte, nur Quiz, beides) sowie die Checkbox-Sperre selbst wurden live über die UI verifiziert (siehe Architekturplanung Abschnitt 13), nicht zusätzlich als eigener Frontend-Test

## Offene, bewusst nicht terminierte Themen

Diese Punkte sind laut Anforderungskatalog (Abschnitt 10) bewusst ohne festen Auslöser in diesem Plan und werden erst aufgenommen, wenn ihre jeweilige Bedingung eintritt:

- Schulzentrierter Einwilligungsweg / schulische IT-Anforderungen — erst falls Schulen aktiv als Kanal hinzukommen
- JMStV-Jugendschutzbeauftragte:r-Pflicht — erst wenn Nutzerzahlen/Reichweite absehbar sind
- Konkrete Infrastruktur für das selbst gehostete KI-Modell — wird laut Architekturplanung erst kurz vor Iteration 6 festgelegt
